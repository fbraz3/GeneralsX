# August 2026

> [!NOTE]
> **AI-Generated Content Disclosure**: This worklog is automatically generated and maintained by AI coding agents to document daily progress, debugging sessions, and technical decisions.

## 28/08/2026
### Restore NGMP Cross-Platform Builds
- Routed the statistics implementation through the existing NGMP JSON compatibility wrapper. This preserves the bundled offline header for Flatpak builds and isolates legacy `snprintf` macros from nlohmann/json on MSVC.

## 27/08/2026
### NGMP: Simultaneous Game Start and Post-Game Statistics Reporting
- **Simultaneous Game Start (`START_GAME`) (`OnlineServices_Manager.h`, `OnlineServices_Manager.cpp`, `WOLGameSetupMenu.cpp`)**:
  - Added `EVENT_GAME_START` to `NGMPEvent::Type` and handled WebSocket `msg_id=13` (`START_GAME`) in `NGMP_OnlineServicesManager::update()` by dispatching `EVENT_GAME_START` to the main UI thread.
  - Updated `StartPressed()` under `SAGE_USE_NGMP`: when the host clicks Start, the client broadcasts `START_GAME` (`msg_id=13`) via WebSocket and initiates local match launch (`TheNGMPGame->startGame(0)`).
  - Added `EVENT_GAME_START` handling in `WOLGameSetupMenuUpdate()`: guests receiving the `START_GAME` broadcast sync room state and launch `TheNGMPGame->startGame(0)` simultaneously with the host.
  - Refactored `buttonStartID` click handler under `SAGE_USE_NGMP` to correctly distinguish between host and guest actions without relying on legacy GameSpy stubs.
- **Post-Game Statistics & Match Outcome Reporting (`OnlineServices_StatsInterface.cpp`)**:
  - Implemented full `CommitMyOutcome()` method sending an asynchronous HTTP POST with player match statistics (`buildings_built`, `buildings_killed`, `buildings_lost`, `units_built`, `units_killed`, `units_lost`, `total_money`, `won`, `side`, `match_id`, `desynced`) to the `{API}/Lobby/Outcome` endpoint.
  - Linked authentication bearer token and libcurl response handling.

### Refine CodeRabbit and Commit Rules for Co-Authors and Trailers
- Clarified Conventional Commits rules in `.coderabbit.yaml` and `AGENTS.md` to prevent false-positive errors on commit trailers.
- Restricted the prohibition of `@` symbols strictly to commit/PR types and subjects (e.g., preventing `@feat:` or `@refactor:`), while explicitly permitting `@` in commit bodies and footers for emails, mentions, and `Co-authored-by` trailers.
- Scoped `auto_title_instructions` strictly to PR title generation.

## 26/08/2026
### Correct Compressed DDS Mip Offsets
- Calculated every retained and skipped DXT mip size from its own rounded block dimensions instead of dividing the preceding size.
- Applied the offset correction to both Generals and Zero Hour, including volume-texture depth reduction.

### Make Unix `itoa` Output Portable
- Added the required compatibility-change annotation to the direct, null-terminated `itoa` conversion.

## 25/08/2026
### Custom Match Lobby List & Server Platform/Anti-Cheat Cleanup
- **Clean Lobby List Rendering & Column Alignment (`WOLLobbyMenu.cpp`)**:
  - Restored clean, standard ListBox columns (Name, Map, Players count `1/4`, Password padlock icon) without rigid image column clipping.
  - Kept game/lobby titles focused on region and game name (`[SA] Minha Sala`).
- **Standardized OS Platform Tags in Player Nicks (`LobbyManager.cs`, `Constants.cs`, `appsettings.json`)**:
  - Attached OS platform tags to player display names at the end of the nick (`[MAC]`, `[LNX]`, `[WIN]`), displaying cleanly across the Lobby user list, Chat messages, and Staging Room slots.
- **Anti-Cheat ID Correction (`OnlineServices_Manager.cpp`)**:
  - **Issue**: Server was attaching `[AC]` to all GeneralsX rooms by default.
  - **Root Cause**: Client was sending `"anticheat_id": 0`, which the server interpreted as `EKnownAnticheatID::GO_INTEGRATED_AC` (Windows Integrated Anti-Cheat).
  - **Fix**: Updated `createLobbyAsync` and `joinLobbyAsync` payloads to send `"anticheat_id": -1` (`EKnownAnticheatID::NONE`), preventing improper `[AC]` tags for GeneralsX clients.

### Universal Return / Enter Key Submission in GUI Text Entry & Popups
- **Universal Enter Key Support (`GadgetTextEntry.cpp`)**:
  - Uncommented `case KEY_ENTER:` and `case KEY_KPENTER:` in `GadgetTextEntry::GWM_CHAR`, enabling seamless Enter/Return key submission across Lobby chat, Staging Room chat, Password popup, and In-Game chat on macOS, Linux, and Windows.
- **Backport Password Popup OK Button and Enter Key to Generals Base Game (`PopupJoinGame.cpp`)**:
  - Added OK button and `KEY_ENTER` / `KEY_KPENTER` event handling to `PopupJoinGameInit`, `PopupJoinGameInput`, and `PopupJoinGameSystem` in the base game codebase.

### Password Padlock Icon & GameServer GeoIP Database Integration
- **Password Padlock Icon (`WOLLobbyMenu.cpp`)**:
  - Updated `RefreshNGMPGameListBoxes` so passworded lobbies render the native `Password` padlock sprite in `COLUMN_PASSWORD` (column 4) using `GadgetListBoxAddEntryImage`.
- **GameServer GeoLite2-City Database Installation (`WebSocketController.cs`, `appsettings.json`)**:
  - Downloaded and deployed `GeoLite2-City.mmdb` on the remote game server (`192.168.1.120`).
  - Added configurable GeoIP fallback and continent/platform/anti-cheat visibility flags to `appsettings.json`.

### NGMP: Staging Room Faction/Color Replication, AI Slot Configuration, and Host Migration UI Unlock
- **Guest Faction, Color, and Team Replication to Host (`WOLGameSetupMenu.cpp`, `OnlineServices_LobbyInterface.cpp`)**:
  - **Issue**: When a guest selected a faction/army, color, or team in the staging room, the change was never sent to the backend and failed to replicate to the host.
  - **Root Cause**: `handlePlayerTemplateSelection()`, `handleColorSelection()`, and `handleTeamSelection()` were querying `TheGameSpyInfo->getCurrentStagingRoom()` and checking `slot->isPlayer(TheGameSpyInfo->getLocalName())`. In NGMP, `TheGameSpyInfo` is an empty stub, causing the handlers to exit before invoking `pLobbyInterface->UpdateCurrentLobby_MySide/MyColor/MyTeam()`.
  - **Fix**: Re-routed handlers under `SAGE_USE_NGMP` to retrieve `myGame` from `TheNGMPGame` / `pLobbyInterface->GetCurrentGame()` and check `index == myGame->getLocalSlotNum()`.
- **Host AI Slot Management & Full AI Faction/Color/Team Backend Dispatch (`WOLGameSetupMenu.cpp`, `OnlineServices_Manager.h`, `OnlineServices_Manager.cpp`, `OnlineServices_LobbyInterface.cpp`)**:
  - **Issue**: The host could open and close slots, but selecting CPU/AI (Easy AI, Med AI, Brutal AI) did nothing.
  - **Root Cause**: In `GCM_SELECTED`, the `comboBoxPlayerID[i]` click handler was guarded with `TheGameSpyInfo->amIHost()`, which returned `FALSE` in NGMP. Furthermore, `UpdateCurrentLobby_AISide/AIColor/AITeam/AIStartPos` were only updating local in-memory slots without sending HTTP updates (`AI_SIDE = 13`, `AI_COLOR = 14`, `AI_TEAM = 15`, `AI_START_POS = 16`) to the backend.
  - **Fix**: Updated `GCM_SELECTED` to use `myGame->amIHost()` and implemented full backend POST requests for `updateLobbyAISide`, `updateLobbyAIColor`, `updateLobbyAITeam`, and `updateLobbyAIStartPos` in `OnlineServices_Manager`. Also ensured `PopulatePlayerTemplateComboBox` is populated for AI slots.
- **Lobby Destruction and Staging Room Disband on Host Departure (`OnlineServices_Manager.cpp`, GameServer `LobbyManager.cs`)**:
  - **Issue**: When the host left the room, attempting in-room host migration caused permission errors (HTTP 401s) on subsequent host POST updates and left "ghost" lobbies showing in the server list.
  - **Solution**: Reverted to the classic RTS/GameSpy pattern where the host owns the room. When the host leaves:
    1. GameServer immediately deletes the lobby (`OnLobbyNeedsDestroyed?.Invoke(this)`).
    2. Clients detect host disconnection in `msg_id == 18` or `requestLobbyDetailsAsync` and fire `EVENT_LOBBY_LEFT`, cleanly returning all guests to the Custom Match lobby list (`WOLCustomLobby.wnd`).

## 24/08/2026
### Stabilize Shrouded Shoreline Alpha
- Traced shadow volume/decal projection, terrain LOD/filtering, water shoreline blend/depth, lightmap, and particle render paths.
- Fixed soft-water shoreline tracks to explicitly modulate their alpha with the shroud texture instead of inheriting stale texture-stage alpha arguments.
- Restored the pass's stage-1 color and alpha operations after rendering so its local shroud modulation cannot affect the next draw.
- Kept the change in the shared renderer so Generals and Zero Hour use identical rendering behavior without affecting simulation or effect timing.

### Preserve Control Bar Card Aspect Ratios on Widescreen Displays
- Rescaled command buttons, selected-unit cameos, upgrade icons, and production queue buttons uniformly on displays wider than 4:3.
- Extended the same correction to special-power shortcut card backgrounds, button hit regions, and overlays.
- Kept the compact card groups centered inside their existing stretched control bar regions without changing full-width HUD artwork.
- Built both Generals and Zero Hour successfully with the macOS Vulkan preset.
- Verified the command-card and selected-unit layout at 3440x1440 and completed a multiplayer replay without a synchronization mismatch.

### Integrate Renderer Quality and Texture Fidelity Fixes
- Added capability-aware MSAA fallback and anisotropy clamping, with linear filtering when anisotropy is unavailable.
- Preserved explicit and authored mip counts, generated complete rectangular mip tails from the preceding filtered level, and kept duplicated Generals/Zero Hour DDS metadata handling aligned.
- Preserved DXT1 fallback alpha and authored skinned-mesh diffuse colors while removing the global lighting/material override that leaked render state across draws.
- Fixed sparse DDS thumbnails to select only valid authored levels, reject formats without a complete decoder, and downsample narrow or oversized DXT1/DXT5 levels into bounded storage.
- Validated deterministic edge cases, both WW3D targets, and full macOS ARM64 builds for Generals and Zero Hour.

### NGMP Game Server Fork Migration, Staging Room Synchronization, and WebSocket Alignment
- **Game Server Submodule Fork Migration**:
  - Reconfigured `.gitmodules` and `references/GameServer` submodule to point to the project's official fork `git@github.com:fbraz3/GeneralsOnlineGameServer.git`.
  - Established a repeatable deployment and maintenance workflow (local commits on fork -> push -> remote sync via SSH on `192.168.1.120` -> `dotnet publish -c Debug` -> systemd restart).
- **Backend Authentication & Tick Timeout Config**:
  - Added configurable tick timeout via `appsettings.json` (`WebSocket:TickTimeoutMs`) and `NGMP_WS_TICK_TIMEOUT_MS` environment variable.
  - Fixed `CheckLoginController.cs` in `#if DEBUG` by registering `PendingLoginManager.AddPendingLogin(gameCode)` prior to `UpdatePendingLogin`, enabling the `ILOVECODE` bypass to issue fully valid JWT authentication tokens during local testing.
- **WebSocket Send Concurrency, Token Isolation & GeoIP Safe Init**:
  - Isolated `UserWebSocketInstance.SendAsync` from outer tick cancellation tokens, preventing premature `OperationCanceledException` during concurrent 5ms tick iterations.
  - Added strict `WebSocketState.Open` verification before and after acquiring `m_SendLock`.
  - Converted `GeoIpReader` in `WebSocketController.cs` to a safe defensive loader with `File.Exists` check, preventing static constructor exceptions (`TypeInitializationException`) from aborting all WebSocket message parsing.
- **Staging Room Synchronization & State Replication Fixes (`WOLGameSetupMenu.cpp`, `NGMPGame.h`)**:
  - **Issue**: Guests joining a lobby showed "NOMAP", closed slots, and default money because `WOLDisplayGameOptions()` and `WOLDisplaySlotList()` were reading from the unpopulated legacy `TheGameSpyInfo->getCurrentStagingRoom()` instead of `TheNGMPGame` where `SyncWithLobby` updates the state.
  - **Fix**: Re-routed `WOLDisplayGameOptions()`, `WOLDisplaySlotList()`, and `WOLPositionStartSpots()` to `TheNGMPGame` under `SAGE_USE_NGMP`.
  - Fixed host check to use `TheNGMPGame->amIHost()` and ensured both host and guest trigger full UI display updates on init and `EVENT_PLAYERS_UPDATED`.
  - Added `AsciiString` overload for `NGMPGameSlot::setPingString()`.
- **Lobby Join Error Recovery & Guest UI Unfreeze (`WOLLobbyMenu.cpp`, `OnlineServices_Manager.cpp`, `OnlineServices_Manager.h`)**:
  - **Issue**: When a guest failed to join or create a lobby (e.g. host deleted room or HTTP error), `joinLobbyAsync` / `createLobbyAsync` silently failed without notifying the UI thread. As a result, `s_tryingToHostOrJoin` remained `TRUE`, permanently disabling all UI buttons (BACK, JOIN, etc.).
  - **Fix**: Added `EVENT_LOBBY_JOIN_FAILED` and `EVENT_LOBBY_CREATE_FAILED` to `NGMPEvent`. On failure, `OnlineServices_Manager` posts the failure event, `WOLLobbyMenuUpdate` resets `SetLobbyAttemptHostJoin(FALSE)`, and pops an error message dialog (`GUI:JoinFailedDefault`). Also prevented sending empty `"password"` fields in join payload.
- **SIGSEGV Crash Prevention on Quick Pop & Engine Shutdown (`WOLLobbyMenu.cpp`, `WOLGameSetupMenu.cpp`)**:
  - **Issue**: `WOLLobbyMenuShutdown` and `WOLGameSetupMenuShutdown` blindly dereferenced `*(Bool *)userData`. When `TheShell->popImmediate()` or engine shutdown pops layouts, `userData` is passed as `nullptr`, triggering a fatal `SIGSEGV` crash.
  - **Fix**: Replaced raw dereference with defensive check: `Bool popImmediate = userData ? *(Bool *)userData : TRUE;`.
- **Map Preview Texture & Start Spots Resolution Fix (`MapUtil.cpp`, `SkirmishGameOptionsMenu.cpp`, `WOLGameSetupMenu.cpp`)**:
  - **Issue**: Map preview displayed an orange question mark `?` (`UnknownMap`) in staging rooms because `WOLPositionStartSpots()` looked up maps by raw name without fallback, and `WOLGameSetupMenuInit()` only set map metadata on legacy `game` instead of `TheNGMPGame`.
  - **Fix**: Enhanced `MapCache::findMap()` with path normalization (`/` vs `\`), display name matching, and filename suffix fallback. Updated `WOLGameSetupMenuInit()` to synchronize map metadata to `TheNGMPGame`. Updated `positionStartSpots()` to resolve maps via `TheMapCache->findMap()`.
- **Staging Room Options, Starting Spots, and Host Leave Synchronization (`WOLGameSetupMenu.cpp`, `SkirmishGameOptionsMenu.cpp`, `OnlineServices_Manager.cpp`, `WOLLobbyMenu.cpp`)**:
  - **Start Spot Numbers on Minimap**: `updateMapStartSpots()` was performing direct string match in `TheMapCache` which failed for non-canonical paths and hid all numbered start spot buttons (`1`, `2`, ...). Replaced with `TheMapCache->findMap(myGame->getMap())` and passed `TheNGMPGame` to `positionStartSpots()`.
  - **Guest Start Spot Selection**: In `WOLGameSetupMenuInit()`, `buttonMapStartPosition[i]->winEnable(FALSE)` was disabling the start buttons for non-host clients. Guarded with `#if !defined(SAGE_USE_NGMP)` and updated `handleStartPositionSelection()`, `getNextSelectablePlayer()`, and `GBM_SELECTED` / `GBM_SELECTED_RIGHT` handlers to route through `TheNGMPGame` and `pLobbyInterface->UpdateCurrentLobby_MyStartPos(startPos)`.
  - **Passworded Room Join Flow (`WOLLobbyMenu.cpp`)**: When clicking "Join" or double-clicking a passworded lobby, the client previously bypassed the password popup and submitted an empty password, failing with HTTP 401. Updated to open `GameSpyOpenOverlay(GSOVERLAY_GAMEPASSWORD)` and stage `entry` in `pLobbyInterface->SetLobbyTryingToJoin()` so the user can enter the password and submit `joinLobbyAsync()`.
  - **Starting Cash Selection Jitter**: Setting `GadgetComboBoxSetSelectedPos(..., TRUE)` inside `WOLDisplayGameOptions()` was firing synthetic selection events on every frame update in a feedback loop. Changed to pass `FALSE` and immediately synchronized local `TheNGMPGame->setStartingCash(startingCash)`.
  - **Superweapon Restriction Toggle**: Updated `handleLimitSuperweaponsClick()` to immediately update `TheNGMPGame->setSuperweaponRestriction()` on the host, preventing the UI refresh loop from instantly reverting the checkbox to unchecked.
  - **Seamless Staging Room Host Migration (`OnlineServices_Manager.cpp`, `OnlineServices_Manager.h`, `WOLGameSetupMenu.cpp`)**:
    - **Issue**: When the host exited a staging room, the server migrated ownership to the guest (`DoHostMigration()`), but because the guest client previously cleared its lobby state or was force-exited, the lobby remained alive on the server with 1/2 players ("phantom lobby").
    - **Fix**: Integrated official Host Migration support in strict parity with `references/GameClient`. Added `EVENT_HOST_MIGRATED` to `NGMPEvent`. When ownership transfers, `WOLGameSetupMenuUpdate` reconfigures the promoted guest to host mode (unlocking map selection, starting cash, superweapons toggle, and changing "Accept" to "Start"). Fixed `requestLobbyListAsync` to prevent prematurely zeroing `m_currentLobbyId` during server room transitions. Ensured `DELETE /Lobby/{id}` is sent when the remaining player leaves, cleanly destroying empty lobbies on the server.
- **Eliminated Chat Duplication in Lobby and Staging Room**:
  - Removed manual local echo from `buttonChatID` and `GEM_EDIT_DONE` across both `WOLLobbyMenu.cpp` and `WOLGameSetupMenu.cpp`, allowing the server broadcast (`msg_id: 2` and `msg_id: 11`) to cleanly render messages exactly once for all players.
- **Database Schema Audit & Missing Anti-Cheat Tables**:
  - Identified 4 missing Anti-Cheat review tables in upstream `Database_Structure/structure.sql` that caused EF Core exceptions on every service tick: `ac_reviews_crc`, `ac_reviews_modules`, `ac_reviews_new_account_games`, and `ac_reviews_probes`.
  - Created the 4 missing tables on the production MariaDB database (`dbname`) and added their full DDL definitions to `structure.sql` (commit `7f442c1`).
  - Audited all 19 entities and 133 fields/columns across C# EF Core mappings, `structure.sql`, and the live database, achieving 100% alignment.

## 22/08/2026
### Fix Null Dereference Crash in PopulatePlayerInfoWindows When Clicking Online
- **Issue**: Clicking "Online" on main menu crashed with `EXC_BAD_ACCESS (SIGSEGV)` at `0x0000000000000004` on macOS ARM64 in `PopulatePlayerInfoWindows(AsciiString) + 252 -> UpdateLocalPlayerStats() + 104 -> WOLWelcomeMenuInit(WindowLayout*, void*)`.
- **Root Cause**:
  - `PopulatePlayerInfoWindows` and `WOLLobbyMenu` accessed `TheRankPointValues->m_ranks[i + 1]` directly without null-checking `TheRankPointValues`.
  - When starting under NGMP mode, `SetUpGameSpy()` was replaced and `TheRankPointValues`, `TheGameSpyConfig`, and `TheLadderList` were never instantiated, leaving `TheRankPointValues` as `nullptr`.
- **Fix**:
  - Instantiated `TheGameSpyConfig`, `TheGameSpyInfo`, `TheRankPointValues`, and `TheLadderList` singletons in `NGMP_OnlineServicesManager::init()`, and provided corresponding teardown in `shutdown()`.
  - Added null checks (`if (TheRankPointValues)`) and array bounds checks (`i + 1 < MAX_RANKS`) in `PopupPlayerInfo.cpp` and `WOLLobbyMenu.cpp`.
  - Safely guarded `ProgressBarRank` calculation to prevent division-by-zero if rank step ranges are empty.
  - Backported fixes to base game `GeneralsX` in `PopupPlayerInfo.cpp` and `WOLLobbyMenu.cpp`.
  - Verified clean compilation of both `GeneralsXZH` and `GeneralsX` on macOS.

### Fix Linux Flatpak Build Script Permissions in CI
- **Issue**: GitHub Actions run `32551742748` failed on `Build Linux Flatpak / generalsxzh@linux64-deploy` and `generalsx@linux64-deploy` with `Permission denied` and exit code 126 on `./scripts/build/linux/build-linux-flatpak.sh`.
- **Root Cause**: `scripts/build/linux/build-linux-flatpak.sh` had file mode `100644` in git (missing executable `+x` permission).
- **Fix**:
  - Updated git file mode with `chmod +x` / `git update-index --chmod=+x` across shell scripts.
  - Updated `.github/workflows/build-linux-flatpak.yml` to invoke `bash ./scripts/build/linux/build-linux-flatpak.sh` explicitly for resilience.

### CodeRabbit Configuration for Automated PR Reviews
- Configured `.coderabbit.yaml` in repository root for automated PR reviews and CI checks.
- Tailored review instructions to enforce GeneralsX architectural standards:
  - C++ platform layer isolation (SDL3/DXVK in `Core/GameEngineDevice/`, no native Win32/Cocoa/raw POSIX in game logic).
  - Cross-platform determinism (macOS ARM64 vs Linux x86_64) enforcing `WWMath` wrappers, division NaN/Inf safety guards, and `ScopedFPUGuard`.
  - Audio backend parity (OpenAL / MiniAudio).
  - Legacy VC6 `#ifdef` compatibility guards.
  - Conventional Commits format enforcement and prohibition of `@` symbols in commit and PR titles.
  - User-facing code annotation format `// GeneralsX @keyword author DD/MM/YYYY Description`.
  - Pre-merge check for author-level commit squash policy (exempting upstream sync PRs from thesuperhackers to preserve original contributor history).
- Configured static analysis and security tools integration (clang, cppcheck, shellcheck, markdownlint, yamllint, actionlint, zizmor, gitleaks, trufflehog).
- Enabled review poem and ensured schema compliance for `tone_instructions` length limit (<= 250 chars).
- Linked CodeGuidelines knowledge base with `AGENTS.md`, `.github/instructions/`, and `CONTRIBUTING.md`.

## 21/08/2026
### Restore Windows CI & Linux Flatpak Build Compatibility for NGMP
- **Issue**: GitHub Actions builds failed:
  1. CMake configure step on Windows failed with `Could NOT find CURL (missing: CURL_LIBRARY CURL_INCLUDE_DIR)`.
  2. MSVC compile step for `ScoreScreen.cpp` failed with `fatal error C1083: Cannot open include file: 'SDL3/SDL.h': No such file or directory`.
  3. MSVC compile step for Zero Hour failed with `error C2039: '_snprintf': is not a member of 'std'` inside `nlohmann/detail/input/lexer.hpp` and `binary_reader.hpp`.
  4. MSVC compile step failed with `error C4189: 'res': local variable is initialized but not referenced` in `OnlineServices_Manager.cpp(769)`.
  5. Linux Flatpak sandboxed offline build failed with `error: '_snprintf' was not declared in this scope` inside bundled `json.hpp`.
  6. Windows Replay Tests failed due to `SetUpGameSpy("", "")` spinning up legacy GameSpy networking threads during engine initialization and lack of headless mode guards for NGMP.
- **Root Cause**:
  1. `vcpkg.json` restricted `curl` with `"platform": "!windows"`, preventing vcpkg from providing `curl` on Windows MSVC builds when `SAGE_USE_NGMP` is enabled.
  2. `ScoreScreen.cpp` directly included `<SDL3/SDL.h>` for `SDL_OpenURL()`, but Windows native builds use Win32 OS APIs rather than SDL3.
  3. `gamespy/gsplatform.h` defined `#define snprintf _snprintf` on `_WIN32`, causing the preprocessor to rewrite standard C++ `(std::snprintf)` calls inside `nlohmann/json.hpp` into `std::_snprintf`, which does not exist in MSVC's `namespace std`.
  4. In `OnlineServices_Manager.cpp` `deleteLobbyAsync()`, `res` was captured from `curl_easy_perform()` but not evaluated, triggering MSVC's `/WX` unused variable error.
  5. Bundled fallback `json.hpp` contained legacy `_snprintf` edits that failed to resolve on Linux GCC standard library.
  6. `OnlineServices_Init.cpp` called `SetUpGameSpy("", "")`, launching background GameSpy queue and ping threads during headless initialization, while `GameEngine.cpp` unconditionally initialized and updated NGMP even under `-headless`.
- **Fix**:
  - Removed `"platform": "!windows"` from `curl` in `vcpkg.json` to install `curl[ssl,websockets]` across all platforms (Windows, Linux, macOS).
  - Added cross-platform `NGMP::OpenURL(url)` helper in `NGMP_Helpers.h`/`.cpp` using `ShellExecuteA()` on Windows and `SDL_OpenURL()` on Linux/macOS.
  - Replaced direct `<SDL3/SDL.h>` inclusions in `ScoreScreen.cpp` and `OnlineServices_Auth.cpp` with `NGMP::OpenURL()`.
  - Updated `NGMP_Helpers.cpp` `GetStoragePath()` to check `USERPROFILE` environment variable as a fallback when `HOME` is undefined on Windows.
  - Updated `NGMP_json.h` to isolate `nlohmann/json.hpp` by pushing and undefining `snprintf`, `vsnprintf`, `_snprintf`, and `_vsnprintf` macros before header inclusion and popping them after.
  - Checked HTTP response code in `deleteLobbyAsync()` in `OnlineServices_Manager.cpp`, resolving the unused variable warning/error.
  - Restored standard `(std::snprintf)` in bundled fallback `json.hpp` for Linux Flatpak compilation.
  - Guarded `NGMP_OnlineServicesManager` `init()` and `update()` in `GameEngine.cpp` with `!TheGlobalData->m_headless`, and added `shutdown()` to `~GameEngine()`.
  - Replaced `SetUpGameSpy("", "")` in `OnlineServices_Init.cpp` with standalone `TheGameSpyInfo` stub allocation, preventing background GameSpy thread creation.
  - Updated `.github/workflows/build-windows.yml` to collect all installed DLLs from both `vcpkg_installed/x86-windows/bin/*.dll` and `build/win32-vcpkg/vcpkg_installed/x86-windows/bin/*.dll` into runtime artifacts.

## 20/08/2026
### Standardize Release Asset Naming Convention
- Standardized all GitHub release artifact filenames to the format `<OperatingSystem>-<GameVersion>.<extension>`:
  - Linux: `Linux-GeneralsXZH.flatpak` and `Linux-GeneralsX.flatpak`
  - macOS: `macOS-GeneralsXZH.tar.zip` and `macOS-GeneralsX.tar.zip`
  - Windows: `Windows-GeneralsXZH.zip` and `Windows-GeneralsX.zip`
- Updated `.github/workflows/release.yml` normalization step, job outputs, and step summary.
- Synced `docs/WORKDIR/support/RELEASE_PIPELINE.md` and `docs/HOWTO/INSTALLATION.md` with new asset names and added Windows installation steps.

### Include Windows x86 Builds in Release Pipeline
- Added Windows build matrix outputs (`generalsxzh-windows-x86` and `generalsx-windows-x86`) to `.github/workflows/release.yml`.
- Enabled `checkout_ref` input on `.github/workflows/build-windows.yml` to checkout the release tag cleanly.
- Updated `cleanup-tag-on-failure` and `prepare-release` jobs to depend on and clean up after `build-windows`.
- Packaged Windows runtimes into release assets `generalsxzh-windows-x86.zip` and `generalsx-windows-x86.zip` and attached them to GitHub releases.

### Cross-Platform Game Language Auto-Detection from BIG Files
- Fixed Windows crash on startup (`INI_CANT_OPEN_FILE` -> "serious error") on non-English Steam / EA App installations where the Windows Registry lacks the `Language` value.
- Extracted `doesLanguageBigExist` and `tryAutoDetectLanguage` out of `_UNIX` conditional compilation so they are available on all platforms (Windows, Linux, macOS).
- Integrated `tryAutoDetectLanguage` fallback into `GetStringFromRegistry`, `GetStringFromGeneralsRegistry`, and `GetRegistryLanguage` across both Zero Hour (`GeneralsMD`) and base game (`Generals`).
- Enabled automatic detection of localized language packs (`BrazilianZH.big` -> `brazilian`, `GermanZH.big` -> `german`, etc.) on Windows without requiring manual registry edits.

## 19/08/2026
### Enable Windows Build Matrix for Generals Base Game
- Backported native MSVC Windows build support to the Generals base game (`Generals/`):
  - Pointed Win32 device sources to shared `Core/GameEngineDevice/Source/Win32Device/` implementations (`Win32GameEngine.cpp`, `Win32OSDisplay.cpp`, `Win32DIKeyboard.cpp`, `Win32Mouse.cpp`).
  - Synced include guards and `d3d8lib` linkage across `Generals/Code/GameEngine/CMakeLists.txt` and `Generals/Code/CMakeLists.txt`.
  - Removed obsolete shadowing CompatLib headers (`comip.h`, `comutil.h`, `windows.h`) and synced `windows_wrapper.h` / `d3dx8math.h`.
- Expanded `.github/workflows/build-windows.yml` to a matrix building both `generalsxzh` (`GeneralsMD` -> `GeneralsXZH.exe`) and `generalsx` (`Generals` -> `GeneralsX.exe`).
- Updated `ci.yml` to trigger `build-windows` when either Zero Hour or Generals base game changes are detected.
- Converted `assets/generalsx_icon.png` and `assets/generalsx-zh_icon.png` into high-resolution multi-size Windows icon files (`Generals.ico`), packaging crisp icons (16x16 to 256x256) into Windows executables.

### Independent Windows CI Asset Cache & Executable Standardization
- Decoupled Windows replay tests from Linux build/replay workflows by establishing dedicated Windows asset caching (`game-assets-cache-windows-v1`) with self-contained fallback extraction.
- Passed `ASSETS_KEY` secret directly to the Windows replay test workflow.
- Cleaned up temporary cross-OS cache export steps from the Linux replay workflow.
- Standardized Windows executable targets to `GeneralsXZH` (`GeneralsXZH.exe`) and `GeneralsX` (`GeneralsX.exe`) across CMake and CI configurations.

## 18/08/2026
### Make Windows Replay Completion Explicit
- Required every Windows replay test to emit a final matching `Game Time current/total` marker instead of trusting exit code alone.
- Printed replay stdout after verbose stderr diagnostics so the completion marker remains visible at the end of each log.
- Renamed the generated-stub artifact to `generalsxzh-windows-x86-replay-test-only`; `generalsxzh-windows-x86` remains the user-facing runtime.

### Fix Staging Room Synchronization & Segfaults
- **Issue**: The multiplayer staging room failed to synchronize between Host and Client (players not appearing, map/cash not syncing). Additionally, a fatal `SIGSEGV` occurred on macOS/Linux when attempting to change the map.
- **Root Cause Analysis (`OnlineServices_Manager.cpp`, `WOLGameSetupMenu.cpp`)**:
  1. **Thread-Safety Segfault**: The background HTTP polling thread (`requestLobbyDetailsAsync`) was modifying UI state directly (`GameSpyStagingRoom` and `GameSpyGameSlot`) while the main thread was concurrently reading them to render the UI, leading to heap corruption.
  2. **Failed ID Matching**: The background thread wiped player IPs (setting `m_userID` to 0), causing `getLocalSlotNum()` to fail and disable the UI combo boxes for the client.
  3. **Missed Rules Sync**: `WOLGameSetupMenuUpdate` handled the `EVENT_PLAYERS_UPDATED` event to update slots, but never invoked the corresponding `SyncWithLobby` function to synchronize match rules (e.g., cash, superweapons, map).
- **Fix**:
  - **Removed Async Mutations**: Stripped all code from `requestLobbyDetailsAsync` that mutated `GameSpyStagingRoom` and `GameSpyGameSlot`. The background thread now exclusively updates the internal lobby JSON cache and dispatches `EVENT_PLAYERS_UPDATED`.
  - **Synchronized on Main Thread**: In `WOLGameSetupMenuUpdate()`, added a call to `TheNGMPGame->SyncWithLobby()` *before* `TheNGMPGame->UpdateSlotsFromCurrentLobby()` when processing `EVENT_PLAYERS_UPDATED`. This guarantees that map, cash, and superweapon changes accurately and immediately replicate to all connected clients on the main thread without race conditions.
  - **Initial Seed Load**: Added identical sync calls in `WOLGameSetupMenuInit()` to display the cached room data instantly while the async request resolves.
  - **Compilation Warnings**: Added `override` to `getLocalSlotNum` in `NGMPGame.h` and completed `switch` fallbacks in `WOLGameSetupMenu.cpp` to resolve warnings on `macos-vulkan`.

### NGMP: Multiplayer Staging Room Synchronization & Cross-Platform Path Resolution
- **Map Path Sanitization & Cross-Platform Resolution (`OnlineServices_Manager.cpp`, `OnlineServices_LobbyInterface.cpp`, `PopupHostGame.cpp`)**:
  - Sanitized map file paths in `createLobbyAsync()` and `updateLobbyMap()` by stripping leading directories and extracting just the map filename. This prevents server-side `FixMapPathForGame` from duplicate-concatenating directory paths into invalid strings (`maps/.../maps/...`).
  - Enhanced map resolution in `requestLobbyDetailsAsync()`: implemented a multi-stage lookup in `TheMapCache` (direct search, normalized `/` and `\\` slashes, display name match, and leaf filename fallback) guaranteeing that official and custom maps resolve correctly on Linux, macOS, and Windows.
  - Fixed map metadata and CRC initialization on guests: guests now load map previews, map availability, and starting spots properly, which unlocks the guest's slot customization controls.
- **Roster & Player List Synchronization (`OnlineServices_Manager.cpp`, `WOLGameSetupMenu.cpp`, `WOLLobbyMenu.cpp`)**:
  - Populated `m_lobbyPlayers` with all active members from `requestLobbyDetailsAsync()` whenever lobby updates are received, ensuring both host and guests see all connected players in the custom lobby player list.
  - Identified `localSlotIndex` by matching `m_userId` against `Members[].UserID` during staging room synchronization.
  - Added `EVENT_LOBBY_LEFT` to `NGMPEvent::Type` and handled it in `WOLGameSetupMenuUpdate()`: guests automatically return to the custom lobby screen when the host leaves or closes the room.
- **Build & Deploy Verification**:
  - Built and deployed `GeneralsXZH` successfully on macOS (`macos-vulkan`).

### NGMP: Staging Room Member Population, Reactive Slot Sync & Linux Map List Fix
- **Lobby Members Population & Slot Synchronization (`OnlineServices_Manager.cpp`)**:
  - Identified root cause where `requestLobbyDetailsAsync()` only populated `GameSpyStagingRoom` but left `m_CurrentLobby.members` empty.
  - As a result, `NGMPGame::UpdateSlotsFromCurrentLobby()` saw zero members, causing all `NGMPGameSlot::m_userID` to stay at `-1`, `getLocalSlotNum()` to always return `-1`, and guest slot interaction/option replication to fail.
  - Updated `requestLobbyDetailsAsync()` to populate `m_CurrentLobby.members` with complete `LobbyMemberEntry` data (`user_id`, `display_name`, `side`, `color`, `team`, `startpos`, `has_map`, `m_bIsReady`, `m_SlotState`, `m_SlotIndex`) and update all current lobby metadata on `NGMP_OnlineServices_LobbyInterface`.
- **Reactive Slot Sync on Main Thread (`WOLGameSetupMenu.cpp`)**:
  - In `WOLGameSetupMenuUpdate()`, invoked `TheNGMPGame->UpdateSlotsFromCurrentLobby()` whenever `EVENT_PLAYERS_UPDATED` is dispatched, ensuring local slot indices and human player statuses synchronize across all clients before UI redraw.
- **Linux Map List Empty Fix (`WOLMapSelectMenu.cpp`)**:
  - Fixed a critical ordering issue where `GadgetListBoxSetListLength(mapList, TheMapCache->size())` was called before `TheMapCache->updateCache()`. On Linux first run, `TheMapCache->size()` evaluated to 0, resizing the listbox to 0 capacity and preventing maps from being added to the UI.
  - Reordered cache updating to run before listbox resizing in both `WOLMapSelectMenuInit()` and radio button selection handlers.
- **Build & Deploy Verification**:
  - Built and deployed `GeneralsXZH` on macOS (`macos-vulkan`) with 0 errors.

### Fix Map Select Menu Null Pointer Segfault on Initialization
- **Root Cause Analysis (`WOLMapSelectMenu.cpp`, `GadgetListBox.cpp`)**:
  - In `WOLMapSelectMenuInit()`, `GadgetRadioSetSelection(radioButtonSystemMaps, FALSE)` was being invoked before `mapList` was resolved from `TheWindowManager`.
  - Calling `GadgetRadioSetSelection` triggered `WOLMapSelectMenuSystem` synchronously with `radioButtonSystemMapsID`, calling `GadgetListBoxSetListLength(mapList, ...)`. Because `mapList` was still `nullptr`, it caused an immediate `EXC_BAD_ACCESS (SIGSEGV)` crash when selecting maps.
- **Fix**:
  - Reordered initialization in `WOLMapSelectMenuInit()` to resolve `mapList` and `winMapWindow` before setting radio button states.
  - Added null pointer check `if (!listbox) return;` at the entry of `GadgetListBoxSetListLength()`.
- **Build & Deploy Verification**:
  - Built and deployed `GeneralsXZH` on macOS (`macos-vulkan`) with 0 errors.

## 17/08/2026
### NGMP: Modal Stack Corruption Fix & Staging Room Interactivity Restoration
- **Root Cause Analysis**:
  - `PopupHostGame.cpp` was invoking `GSMessageBoxOk(TheGameText->fetch("GUI:CreatingLobby"), TheGameText->fetch("GUI:LobbyCreationInProgress"), nullptr)`. The keys `GUI:CreatingLobby` and `GUI:LobbyCreationInProgress` did not exist in `.csf` string tables, producing `MISSING: 'GUI:CreatingLobby'` dialog text.
  - When `createGame()` showed `MessageBox.wnd`, it became the top modal in `GameWindowManager` (`m_modalHead`). Immediately following, `GameSpyCloseOverlay(GSOVERLAY_GAMEOPTIONS)` destroyed `parentPopup`.
  - `GameWindowManager::winUnsetModal(GameWindow *window)` previously only checked `if (m_modalHead->window == window)`. Because `parentPopup` was beneath `MessageBox.wnd` in the modal stack, it was never unlinked and was destroyed while still in `m_modalHead->next`.
  - When `MessageBox.wnd` closed, `m_modalHead` became the dangling pointer to the destroyed `parentPopup`. Since `GameWindowManager::winPointInChild` redirects all input checks through `m_modalHead`, every click and keypress in the game was rejected, completely locking all UI controls in `GameSpyGameOptionsMenu.wnd` (army selection, team, color, starting cash, superweapon limit, map preview, chat, and back button).
- **Core Platform Fix (`GameWindowManager.cpp`)**:
  - Rewrote `winUnsetModal(GameWindow *window)` in both `GeneralsMD` and `Generals` base game to traverse the `m_modalHead` linked list and safely unlink `window` wherever it resides in the modal stack.
  - Updated `windowHiding(GameWindow *window)` and `winDestroy(GameWindow *window)` to call `winUnsetModal(window)` unconditionally, eliminating dangling modal pointers when child or sibling popups are destroyed.
- **Overlay & Message Box Polish (`GameSpyOverlay.h/.cpp`, `PopupHostGame.cpp`)**:
  - Declared and implemented `GSMessageBoxCancel(UnicodeString title, UnicodeString message, GameWinMsgBoxFunc cancelFunc)` in `GameSpyOverlay.h/.cpp`.
  - Updated `PopupHostGame.cpp` to use `GSMessageBoxCancel(UnicodeString(L"Creating Lobby"), UnicodeString(L"Lobby Creation is in progress..."), nullptr)`.

### NGMP: Staging Room Map Selection, Match Option Controls & WebSocket Chat Fixes
- **Map Selection Synchronization (`WOLMapSelectMenu.cpp`)**:
  - When the host chooses a new map in `WOLMapSelectMenu.cpp`, updated `TheGameSpyInfo->getCurrentStagingRoom()` with the chosen map name, map CRC, map size, and slot adjustments so that `WOLDisplayGameOptions()` and `WOLDisplaySlotList()` immediately reflect the selected map and preview in the staging room.
  - Forwarded the updated map name and path to `pLobbyInterface->UpdateCurrentLobby_Map()`.
- **Match Option Controls (`WOLGameSetupMenu.cpp`)**:
  - Fixed host permission checks in `InitWOLGameGadgets()` using `pLobbyInterface->IsHost()` instead of legacy `TheGameSpyGame->amIHost()`, correctly enabling `CheckboxLimitSuperweapons` and `ComboBoxStartingCash` for the host upon entering the staging room.
  - Verified in `references/GameClient` that `CheckBoxUseStats` (Record Stats) and `CheckBoxLimitArmies` (Limit Armies) are locked by design inside the staging room and can only be toggled during room creation in `PopupHostGame.wnd`.
- **WebSocket Chat & Payload Dispatch (`OnlineServices_WebSocket.cpp`, `OnlineServices_Manager.h/.cpp`, `OnlineServices_LobbyInterface.cpp`, `OnlineServices_RoomsInterface.cpp`)**:
  - Implemented `sendRawWebSocketPayload(const std::string& rawPayload)` in `NGMP_OnlineServicesManager` to send unadulterated JSON payloads directly over the active chat WebSocket session.
  - Re-routed all `SendData_*` methods in `OnlineServices_WebSocket.cpp` (`SendData_LobbyChatMessage`, `SendData_RoomChatMessage`, `SendData_ChangeName`, `SendData_MarkReady`, `SendData_StartGame`, `SendData_CountdownStarted`, `SendData_FriendMessage`) to use `sendRawWebSocketPayload()`, resolving the bug where `sendChatMessage` was nesting JSON strings inside `msg_id: 1` (`NETWORK_ROOM_CHAT_FROM_CLIENT`).
  - Updated `SendChatMessageToCurrentLobby` and `SendChatMessageToCurrentRoom` to dispatch through `GetWebSocket()->SendData_*`.
- **Build & Deploy Validation**:
  - Built and deployed `GeneralsXZH` successfully on macOS (`macos-vulkan`).

## 16/08/2026
### Upstream Sync from TheSuperHackers (`thesuperhackers/main`)
- Created sync branch `thesuperhackers-sync-08-16-2026` and merged upstream `thesuperhackers/main`.
- Reconciled audio subsystem: accepted upstream's `AudioRequest` reference-counted `RefCountPtr<DynamicAudioEventRTS>` and destructor improvements to prevent dangling pointers on looping audio; adapted `MiniAudioManager` and `OpenALAudioManager` to maintain strict cross-platform audio parity.
- Reconciled networking subsystem: unified `ConstructBigCommandList` chunk-based splitting with `NetCommandDataChunk` across `NetPacket.h`, `NetPacket.cpp`, and `Connection.cpp`.
- Maintained platform isolation in `GeneralsMD/Code/Main/CMakeLists.txt` and `Core/Libraries/Source/profile/CMakeLists.txt` (guarded Windows-only `core_debug` dependency).
- Preserved custom CI/CD configuration by rejecting upstream `build-toolchain.yml`.
- Reconciled unified `Core/` file movements (`Shell.h`, `ShellHooks.h`, `ShellMenuScheme.h`, `Shell.cpp`, `ShellMenuScheme.cpp`, and heightmap/scorch additions).
- Verified full clean compilation and linking on macOS (`macos-vulkan` preset) producing both `GeneralsMD/GeneralsXZH` and `Generals/GeneralsX` binaries.

### Validate Cross-Platform Replays on Windows
- Added a protected cross-OS asset cache so fork pull requests can reuse decrypted replay assets without exposing repository secrets.
- Kept generated Miles and Bink stubs limited to the CI-only runtime while distributable Windows artifacts preserve licensed retail DLLs.
- Configured the headless Windows runner's language from its installed language BIG so both Linux- and macOS-recorded replay sets can start against the same assets.
- Standardized new replay wide characters on the retail UTF-16 layout and retained automatic playback of older Unix UTF-32 captures.

### NGMP: Staging Room (WOLGameSetupMenu) & Map Selection (WOLMapSelectMenu) Restoration
- **Staging Room Synchronization (`OnlineServices_Manager.cpp`, `OnlineServices_Manager.h`)**:
  - Implemented `requestLobbyDetailsAsync(int64_t lobbyId)` to query `GET /env/dev/contract/1/Lobby/{id}`, parsing map properties, game options (cash, superweapon limits, observers), and all 8 slot states/members into `TheGameSpyInfo->getCurrentStagingRoom()`.
  - Added handling for WebSocket `msg_id: 6` (`LOBBY_CURRENT_LOBBY_UPDATE`), auto-refreshing staging room state upon server updates.
  - Implemented client-to-server update dispatches for `updateLobbyMap`, `updateLobbyStartingCash`, `updateLobbyLimitSuperweapons`, `updateLobbyMySide`, `updateLobbyMyColor`, `updateLobbyMyStartPos`, `updateLobbyMyTeam`, `updateLobbySlotState`, and `updateLobbyLeave` via `POST /env/dev/contract/1/Lobby/{id}`.
- **Map Selection Menu Fixes (`WOLMapSelectMenu.cpp`)**:
  - Fixed `WOLMapSelectMenuInit` to read the active map from `TheGameSpyInfo->getCurrentStagingRoom()` instead of unpopulated `TheGameSpyGame`, defaulting to `getDefaultMap(TRUE)`.
  - Corrected radio button initialization and toggling (`GadgetRadioSetSelection(..., TRUE)`) so official and custom maps lists populate correctly without empty boxes.
  - Updated `buttonOK` handler to update `TheGameSpyInfo->getCurrentStagingRoom()` and dispatch `updateLobbyMap` to the NGMP backend.
- **Game Setup Menu Fixes (`WOLGameSetupMenu.cpp`)**:
  - Fixed `WOLGameSetupMenuInit` map caching: replaced `TheMapCache->find(lowerMap)` with `TheMapCache->findMap(mapToFind)` to resolve relative map file paths and display map preview thumbnails correctly.
  - Integrated `EVENT_PLAYERS_UPDATED` handling in `WOLGameSetupMenuUpdate` to refresh UI slots (`WOLDisplaySlotList()`) and game settings (`WOLDisplayGameOptions()`).
  - Hooked host/client GUI controls (color, team, army/faction, starting cash, superweapon limit, slot state change, room exit) to dispatch corresponding updates to the NGMP backend.
- **Build & Deployment**: Verified zero-error compilation and deployed `GeneralsXZH` and `GeneralsX` on macOS (`macos-vulkan`).

### NGMP: Architecture Bridge & Menu Alignment with Reference Repository
- **Thin Adapter Architecture Bridge (`NGMP_interfaces.h`, `NGMPGame.h`, `NGMPGame.cpp`, `OnlineServices_Manager.h`)**:
  - Implemented `TheNGMPGame` (`NGMPGame` : `GameInfo`) and `NGMPGameSlot` (`NGMPGameSlot` : `GameSlot`) matching `references/GameClient`.
  - Added typed interface accessor `NGMP_OnlineServicesManager::GetInterface<T>()` and singleton accessor `NGMP_OnlineServicesManager::GetInstance()`.
  - Added sub-interface adapter headers and implementations: `OnlineServices_Auth.h/.cpp`, `OnlineServices_LobbyInterface.h/.cpp`, `OnlineServices_RoomsInterface.h/.cpp`, `OnlineServices_StatsInterface.h/.cpp`, `OnlineServices_SocialInterface.h/.cpp`.
  - Added `WebSocket` wrapper class exposing helper methods (`SendData_ChangeName`, `SendData_RoomChatMessage`, `SendData_LobbyChatMessage`, `SendData_JoinNetworkRoom`, `SendData_LeaveNetworkRoom`, `SendData_MarkReady`, `SendData_StartGame`, `SendData_CountdownStarted`).
- **Map Selection Menu Alignment (`WOLMapSelectMenu.cpp`)**:
  - Switched from GameSpy staging room to `TheNGMPGame` and `NGMP_OnlineServices_LobbyInterface`.
  - Used `TheNGMPGame->getMap()` and updated map selection to notify backend via `pLobbyInterface->UpdateCurrentLobby_Map()`.
  - Added listbox length expansion (`GadgetListBoxSetListLength`) to show all maps beyond the legacy 1200 map limit.
- **Custom Lobby Menu Alignment (`WOLLobbyMenu.cpp`)**:
  - Added `/name` and `/nick` chat slash commands to change player display name in real-time via WebSocket.
- **Build Verification**:
  - Verified clean builds for both `GeneralsMD` (`GeneralsXZH`) and `Generals` (`GeneralsX`) on macOS (`macos-vulkan`).

### NGMP: Lobby & Match Room Gap Resolution (Callbacks, Filtering, and Staging Room Parity)
- **Lobby Menu Callbacks & Cleanup (`WOLLobbyMenu.cpp`)**:
  - Implemented `NGMP_WOLLobbyMenu_CreateLobbyCallback` and `NGMP_WOLLobbyMenu_JoinLobbyCallback` to transition to the staging room (`GameSpyGameOptionsMenu.wnd`).
  - Added callback registration (`RegisterForCreateLobbyCallback`, `RegisterForJoinLobbyCallback`, `RegisterForChatCallback`, `RegisterForRosterNeedsRefreshCallback`) in `WOLLobbyMenuInit`.
  - Added callback deregistration on menu exit in `WOLLobbyMenuShutdown`.

## 15/08/2026
### Match Cross-Platform Bezier Trajectories
- Fixed deterministic projectile paths after GLM's platform-dependent matrix and dot-product evaluation order caused divergence. When `USE_DETERMINISTIC_MATH` is enabled, GLM builds now use explicitly ordered D3DX-compatible scalar operations.
- Applied the same operation order to GLM transforms and dot products so projectile positions remain bit-identical across macOS and Windows.

### Fix Shared Tunnel and Cave Network Exits
- Fixed passenger exit commands being rejected when a unit entered one tunnel or cave endpoint and was ordered out through another endpoint in the same player's shared network.
- Kept strict rejection for unrelated containers, separate cave networks, and endpoints controlled by another player.
- Applied the shared gameplay fix to Zero Hour and Generals.

### Restore Deterministic Native Windows Build
- Repaired the experimental MSVC x86 Zero Hour build and enabled the fdlibm-backed deterministic math path used by Linux and macOS.
- Restored native Win32, DirectInput, DirectX 8, Miles, Bink, and VFW source paths without exposing POSIX compatibility headers to Windows SDK includes.
- Added Windows CI that packages the runtime and validates both Linux- and macOS-recorded replays without replacing the licensed installation's retail Miles and Bink DLLs.
- Confirmed a real macOS ARM64 and Windows x86 multiplayer match completed without a synchronization error.

### NGMP: Lobby List Deserialization, WebSocket Fragmentation, and Auto-Refresh Fixes
- **Lobby List Deserialization (`OnlineServices_Manager.cpp`)**: Updated `requestLobbyListAsync()` to support PascalCase keys (`LobbyID`, `Name`, `MapName`, `NumCurrentPlayers`, `MaxPlayers`, `IsPassworded`) returned by the C# Kestrel backend while preserving backwards-compatibility fallbacks for camelCase/snake_case. This resolves the issue where created custom lobbies were receiving an ID of 0 and failed to display or join.
- **WebSocket Fragmented Frame Reassembly (`OnlineServices_WebSocket.cpp`)**: Aligned WebSocket receive loop with `references/GameClient`, accumulating partial frames into a buffer (`std::vector<char> partialBuffer`) and verifying `!(meta->flags & CURLWS_CONT) && (meta->bytesleft == 0)` before passing the complete message payload to the JSON parser.
- **Custom Lobby Auto-Refresh & Join Handling (`WOLLobbyMenu.cpp`)**:
  - Removed the `ngmpRoomConfirmed` blocking condition from `refreshGameList()`, allowing periodic 10-second background polling of lobbies from the server.
  - Added immediate asynchronous lobby list fetch (`requestLobbyListAsync()`) on `WOLLobbyMenuInit`.
  - Updated `RefreshNGMPGameListBoxes` to set `ItemData` to the actual `lobby.id`, and updated the join button click handler to look up lobbies directly by ID rather than listbox index.
- **Client/Server Architecture Isolation**: Enforced documentation constraint that the NGMP backend server is considered 100% functional and must not be altered, ensuring all fixes and protocol conformances remain strictly on the client side (`GeneralsMD`).
- **Build & Deploy Validation**: Built and deployed `GeneralsXZH` and `GeneralsX` clean on macOS Vulkan preset (`macos-vulkan`).

## 14/08/2026
### Restore Windows LAN Map Compatibility
- Kept spaces literal when serializing map names so TheSuperHackers Windows builds can resolve official maps such as Twilight Flame.
- Preserved percent decoding for compatibility with existing GeneralsX replays and reserved-character map names.

### NGMP: Custom Lobby Player Roster Population & Null Safety
- **Player Roster in Custom Lobby (`WOLLobbyMenu.cpp`)**: Added `#if defined(SAGE_USE_NGMP)` implementation for `PopulateLobbyPlayerListbox()` that iterates `NGMP_OnlineServicesManager::getLobbyPlayers()`, safely inserting each player and rank image into `ListboxPlayers`. Added immediate call to `PopulateLobbyPlayerListbox()` in `WOLLobbyMenuInit` with a local user fallback.
- **Null Safety in Legacy Code Paths**: Added null pointer check for `TheGameSpyConfig` before invoking `isPlayerVIP` in `WOLLobbyMenu.cpp` to prevent virtual method crashes on uninitialized GameSpy configs.
- **WebSocket JSON Parsing & Diagnostic Logging (`OnlineServices_Manager.cpp`)**: Extended `msg_id: 4` (`NETWORK_ROOM_MEMBER_LIST_UPDATE`) deserialization to gracefully handle varying case conventions (`UserID`/`user_id`, `Name`/`display_name`, `IsAdmin`/`is_admin`) and added real-time diagnostic console logging for incoming WebSocket frames and parsed rosters.
- **Classic Voice Line "Welcome to Generals Online" (`WOLWelcomeMenu.cpp`)**: Added `WelcomeToGeneralsOnline` audio event trigger (`DialogEvent` with `euonline.wav`) upon initializing the online welcome screen (`WOLWelcomeMenuInit`), reproducing the retail Westwood/EA online greeting. Gated playback to execute only once per login session (`s_welcomeAudioPlayed`), deferring playback until after initial region selection (`GSOVERLAY_LOCALESELECT`) closes on first login, guarding against shutdown/exit transitions (`!isShuttingDown && !buttonPushed`), and preventing repetitive audio triggers when returning from submenus (Custom Match, Quick Match, Buddies, Player Info). Reset upon completion of return-to-main-menu shutdown transition (`shutdownComplete`). Backported to both Zero Hour and Generals base game.
- **Fix Base Game & Flatpak Offline Builds (`LobbyUtils.cpp`, `NGMP_json.h`, `cmake/ngmp.cmake`)**:
  - Removed `OnlineServices_Manager.h` dependency from shared `Core/GameEngine/Source/GameNetwork/GameSpy/LobbyUtils.cpp` by moving `RefreshNGMPGameListBoxes` directly into `GeneralsMD/.../WOLLobbyMenu.cpp`. This fixes Generals base game builds across macOS and Linux.
  - Bundled single-header `json.hpp` under `GeneralsMD/.../GeneralsOnline/json.hpp` and updated `NGMP_json.h` with fallback header resolution, ensuring successful compilation in offline / sandboxed environments such as Linux Flatpak where FetchContent downloads are restricted (`FETCHCONTENT_FULLY_DISCONNECTED=ON`).
- **Deferred Online Login & CI Server Config Propagation (`OnlineServices_Init.cpp`, `build-macos.yml`, `build-linux-flatpak.yml`, `build-linux-flatpak.sh`, `cmake/config-build.cmake`, `cmake/ngmp.cmake`)**:
  - Removed automatic background login trigger from `OnlineServices_Init.cpp` at engine startup. Authentication and CheckLogin polling are now deferred strictly until the user clicks Multiplayer -> Online (`MainMenu.cpp`).
  - Removed conflicting duplicate NGMP compile definitions in `cmake/config-build.cmake` that were overriding host configuration with a default localhost fallback prior to `cmake/ngmp.cmake` running.
  - Added generation of non-hidden `cmake/ngmp_env.cmake` in `build-linux-flatpak.sh` so sandboxed flatpak-builder builds always copy and include the configured NGMP host, port, and SSL settings.

## 13/08/2026
### NGMP: WebSocket libcurl concurrency fix & Lobby UX Polish
- **WebSocket Thread Safety**: Fixed SIGABRT malloc crash (`___BUG_IN_CLIENT_OF_LIBMALLOC_POINTER_BEING_FREED_WAS_NOT_ALLOCATED`) in `OnlineServices_WebSocket.cpp` by wrapping `curl_ws_recv` with `m_sendMutex`. `libcurl` handles are strictly non-thread-safe, so synchronizing recv/send/disconnect prevents race conditions.
- **Player Identity in Staging Room**: Added `TheGameSpyInfo->setLocalName` and `setLocalProfileID` calls upon `EVENT_AUTH_SUCCESS` in `OnlineServices_Manager.cpp` and `MainMenu.cpp`, ensuring slot 0 displays the logged-in username (`DEV_ACCOUNT_0`).
- **Ping Indicator Visibility**: Following `references/GameClient`, hidden `genericPingWindow` for local player slots in `WOLGameSetupMenu.cpp`.
- **Post-Match Return Flow**: Fixed return transition in `WOLGameSetupMenuInit` after game end under `SAGE_USE_NGMP` to correctly push `Menus/WOLCustomLobby.wnd` instead of falling back to Main Menu.

## 12/08/2026
### NGMP: Segfault fix when clicking "Create Game" (WOLGameSetupMenuInit)
**Root cause**: `WOLLobbyMenuUpdate` was calling `TheShell->pop()` *before* `TheGameSpyInfo->markAsStagingRoomHost()` when handling `EVENT_LOBBY_CREATED`. Because `buttonPushed=true` was set on the same line, `Shell::pop()` triggers `WOLLobbyMenuShutdown` with `popImmediate=TRUE`, which synchronously calls `shutdownComplete` -> `TheShell->push("GameSpyGameOptionsMenu")` -> `WOLGameSetupMenuInit`. At that point, `getCurrentStagingRoom()` returned `nullptr` (since `m_isHosting=FALSE`), causing a SIGSEGV on `game->getSlot(0)`.

**Fix**:
- `WOLLobbyMenu.cpp`: Moved `markAsStagingRoomHost()` / `markAsStagingRoomJoiner()` to execute **before** `TheShell->pop()` in the `EVENT_LOBBY_CREATED` and `EVENT_LOBBY_JOINED` handler.
- `WOLGameSetupMenu.cpp`: Added defensive null-check for `getCurrentStagingRoom()` return value to prevent future crashes if state is somehow lost; logs to stderr and pops back to lobby.
- `PopupHostGame.cpp`: Added diagnostic `fprintf(stderr, ...)` log before `createLobbyAsync` call to trace invocation.

## 11/08/2026
### Upstream Sync from TheSuperHackers (`thesuperhackers/main`)
- Created sync branch `thesuperhackers-sync-08-10-2026` and merged upstream `thesuperhackers/main`.
- Resolved logic conflicts in `GameLogicDispatch.cpp` (preserving upstream timing checks alongside local performance optimizations) and `Recorder.cpp`.
- Resolved build system conflicts in `CMakeLists.txt` across `Core/GameEngineDevice`, `Generals/`, and `GeneralsMD/` while preserving platform isolation (`if(WIN32)` blocks).
- Reconciled file movements into `Core/`: applied local custom fixes (`shouldPause` HUD behavior, GeneralsX watermark, Cyrillic font support) to the unified `Core/` classes (`InGamePopupMessage.cpp`, `W3DMainMenu.cpp`, `W3DGameFont.cpp`).
- Verified full compilation and linking on macOS (`macos-vulkan` preset) for Zero Hour and Generals base game.
- Executed full Linux Docker configure (`docker-configure-linux.sh linux64-deploy`) and build scripts (`docker-build-linux-zh.sh` and `docker-build-linux-generals.sh`), confirming clean compilation and linking of `GeneralsXZH` (280M) and `GeneralsX` (268M) binaries.

### Fix Multi-Monitor Window Placement and HiDPI Window Sizing
- Fixed the game opening on a secondary monitor at an oversized width on multi-monitor macOS setups.
- Root cause was a unit mismatch: `buildFilteredResolutions()` clamps the resolution list against `SDL3_GetNativeDisplaySize()`, which returns `mode->w * pixel_density` (physical pixels), but `SDL3_ApplyWindowModeForRenderConfig()` passed those values straight to `SDL_SetWindowSize()`, which expects logical points. On a 2x Retina display a 3840x2160 render resolution produced a 7680-physical-pixel-wide window — twice the width of the whole desktop.
- The oversized window then spanned both displays, and because SDL resolves a window's display by its center point, `SDL_GetDisplayForWindow()` reported the adjacent monitor and the window migrated there.
- `SDL3_ApplyWindowModeForRenderConfig()` now derives the pixel-to-point ratio from the window itself (`SDL_GetWindowSizeInPixels` / `SDL_GetWindowSize`) rather than the display's `pixel_density`. The drawable is only scaled when the window carries `SDL_WINDOW_HIGH_PIXEL_DENSITY`, which is set on macOS only, so the ratio collapses to 1.0 on other platforms and non-HiDPI behavior is unchanged.
- `SDL_CreateWindow()` takes no position argument and resolves to `SDL_WINDOWPOS_UNDEFINED`, which SDL maps to display index 0 of its own enumeration order rather than the OS primary display. `SDL3Main.cpp` now pins the initial placement to `SDL_GetPrimaryDisplay()` while the window is still hidden, so there is no visible jump and fullscreen no longer inherits the wrong display.
- Added `SDL3_CenterWindowOnCurrentDisplay()`, called after the window is resized. SDL keeps the top-left anchor across `SDL_SetWindowSize()`, so growing from the 1024x768 bootstrap size to the configured resolution previously pushed the window off the bottom and right of the display. Centering is clamped to `SDL_GetDisplayUsableBounds()` so the window stays clear of the macOS menu bar, and anchors to the bounds' top-left when the window is larger than the usable area, which keeps the title bar reachable.
- Positioning failures are logged rather than swallowed; Wayland rejects programmatic window positioning by design, so this is a warning and not an error.
- Applied to both Generals and Zero Hour.

### Known Remaining Issue
- `DX8Wrapper::Resize_And_Position_Window()` (`dx8wrapper.cpp`) hardcodes position `(0,0)` on SDL platforms where the Win32 path centers on the current monitor's work area, and passes physical pixels to the `SetWindowPos` compat shim, which forwards them to `SDL_SetWindowSize()`. The window-mode pass in `W3DDisplay` corrects both afterwards, so the visible result is right, but the window can briefly flash at the wrong geometry. Fixing it at source touches shared `Core/` code and needs Linux validation before it lands.

### NGMP: Authentication & Profile Screen (PopupPlayerInfo) Parity
- Addressed missing globals by defining `g_bForceRelay` and `m_exeCRCOriginal` in `WOLLobbyMenu.cpp` instead of using `extern`.
- Fixed Linux GCC compilation failure caused by GameSpy `min`/`max` macro pollution in `PersistentStorageThread.h`, `OnlineServices_Manager.h`, and `OnlineServices_StatsInterface.h` conflicting with C++ standard template headers (`<functional>`, `<bits/ranges_algo.h>`).
- Fixed `max(1, atof(pTotal))` type mismatch in `WOLWelcomeMenu.cpp` by using `std::max(1.0, atof(pTotal))`.
- Verified successful zero-error build and deployment of `GeneralsXZH` and `GeneralsX` on macOS (`macos-vulkan`).

### NGMP: Full GUI Parity Reconciliation Across All Remaining Menus
- **PopupHostGame (`PopupHostGame.cpp`)**:
  - Saved and restored `LastLobbyName` via `CustomMatchPreferences` string persistence (defaulting to "Generals Online Lobby").
  - Configured `createGame()` under `SAGE_USE_NGMP` to retrieve user-selected map from preferences, query `TheMapCache->findMap(defaultMap)`, retrieve lobby options, and call `pLobbyInterface->CreateLobby()` with `GSMessageBoxOk` progress feedback.
- **PopupPlayerInfo (`PopupPlayerInfo.cpp`)**:
  - Re-routed "Delete Account" button to act as "LOGOUT" when inspecting the local user's account (`lookAtPlayerID == myUserID`).
  - Hooked confirmation modal to call `pAuthInterface->LogoutOfMyAccount()`, triggering `NGMP_OnlineServicesManager::getInstance().logout()`.
- **PopupJoinGame (`PopupJoinGame.cpp`)**:
  - Configured `PopupJoinGameInit` to retrieve `lobbyTryingToJoin` from `pLobbyInterface->GetLobbyTryingToJoin()`.
  - Configured `joinGame(AsciiString password)` to call `pLobbyInterface->JoinLobby(lobbyTryingToJoin, password.str())`.
- **InGameChat (`InGameChat.cpp`)**:
  - Added null check for `TheNGMPGame` before toggling in-game chat.
- **ScoreScreen (`ScoreScreen.cpp`, `OnlineServices_StatsInterface.h/.cpp`, `OnlineServices_LobbyInterface.h`)**:
  - Implemented `CommitMyOutcome()` to record post-match stats and report outcome to the NGMP backend.
  - Added post-match URL link display (`https://www.playgenerals.online/viewmatch?match=<id>`) and configured Continue button to open the link in browser via `SDL_OpenURL()`.
  - Updated `populatePlayerInfo()` to handle `TheNGMPGame` stats tracking without relying on legacy GameSpy message queues.
- **WOLGameSetupMenu (`WOLGameSetupMenu.cpp`)**:
  - Added slash commands `/help`, `/commands`, `/friendsonly`, `/public`, and `/maxcameraheight`.
  - Integrated `pLobbyInterface->SetJoinability()`.
- **WOLBuddyOverlay (`WOLBuddyOverlay.cpp`)**:
  - Updated notification expiration to 5000ms.
  - Implemented in-game anti-cheat buddy chat validation checking `TheNGMPGame->isGameInProgress()` and verifying player slot IDs.
- **DownloadMenu & UserPreferences (`DownloadMenu.cpp`, `UserPreferences.cpp`)**:
  - Added `SetFileName()` and safe division for download progress ratio calculation.
  - Guarded NGMP auth ID preferences with `HAVE_NGMP_PREFS` via standard `__has_include`, maintaining zero-regression compatibility for base game `GeneralsX` builds.
- **Build Verification**:
  - Verified clean compilation and linking of both `GeneralsXZH` and `GeneralsX` on macOS (`macos-vulkan`).

## 13/08/2026
### Stop SagePatch.ini From Overriding a User-Authored GameData.ini
- Reported symptom: `MaxCameraHeight` set in a loose `Data/INI/GameData.ini` had no effect in game.
- Root cause was load order in `GameEngine::init()`. The engine loads `Data\INI\Default\GameData` and `Data\INI\GameData` through `initSubsystem()`, then loads the auto-generated `SagePatch.ini` from the user data dir with `INI_LOAD_OVERWRITE`. SagePatch ships a hardcoded `GameData` block (`MaxCameraHeight = 350.0`, `MinCameraHeight = 100.0`, `EnforceMaxCameraHeight = No`, ...), so it silently overwrote whatever the user had authored.
- The loose `Data/INI/GameData.ini` shadows the BIG-archived copy, because `FileSystem::openFile()` resolves local before archive. The archived original is therefore never a factor once a user file exists, so the fix replays that loose file after `SagePatch.ini`, guarded by `TheLocalFileSystem->doesFileExist()`. Installs without a loose `GameData.ini` keep the previous behavior.
- Reloading is safe: under `INI_LOAD_OVERWRITE`, `GlobalData::parseGameDataDefinition()` is plain field assignment with no `newOverride()` push, and the extra load is not fed to `xferCRC`, so the startup light CRC is unchanged.
- Fixed the generated `~5% more terrain` comment, which was passed as a `fprintf` format string and lost the `%` and the following space in every `SagePatch.ini` written to date.
- Updated the generated header comment to document the precedence rule.
- Known trade-off: a loose `GameData.ini` must be a complete copy of the archived file (it shadows, not merges), so the replay makes *every* key in it authoritative — including stock values the user copied without meaning to change. Concretely, stock pins `FramesPerSecondLimit = 30`, so a user file based on the stock copy reverts SagePatch's 60 FPS default unless the user edits that line too. Keys absent from stock (e.g. `TerrainDrawDistanceScale`) keep their SagePatch values.
- Applied to both Generals and Zero Hour.

## 07/08/2026
### Phase 6: NGMP Global & Player Stats Integration
- Added `requestGlobalStatsAsync` and `requestPlayerStatsAsync` to `NGMP_OnlineServicesManager` to fetch stats from the server REST API.
- Replaced GameSpy polling for global stats and persona stats with the new NGMP async calls in `WOLWelcomeMenu` and `PopupPlayerInfo`.
- Added `GlobalStats` and `PSPlayerStats` caching to `OnlineServices_Manager` under dedicated mutexes for thread safety.
- Fixed the Communicator (buddy overlay) closing automatically by modifying `WOLBuddyOverlay.cpp` and `GameSpyOverlay.cpp` to use `NGMP_OnlineServicesManager::getInstance().isLoggedIn()` instead of legacy GameSpy checks.
- Set up `GameSpyPlayerInfoOverlayUpdate` and `PopupPlayerInfo.cpp` to natively wait for stats using a boolean flag `g_waitingForPlayerStats`, updating the UI re-actively once `EVENT_PLAYER_STATS_RECEIVED` is fired.
- Adapted `PSPlayerStats` deserialization from reference implementation to match GeneralsX layout (removed `elo_rating` and `elo_num_matches`).

## 03/08/2026
### Phase 1: GeneralsOnline NGMP Setup
- Created feature branch `feat/generals-online-ngmp` off `main`.
- Added dedicated subsystem instructions in `.github/instructions/ngmp.instructions.md` with constraints on Windows OS API removal, POSIX/SDL3 abstractions, thread-safe UI queues, and default server endpoint (`ws://192.168.1.120:9001/ws`).
- Registered `ngmp.instructions.md` in `AGENTS.md` instruction context loading table.

### Phases 2-5: NGMP Protocol & Engine Integration
- Integrated `nlohmann/json` and `libcurl` via CMake (`cmake/ngmp.cmake`, `SAGE_USE_NGMP` option).
- Implemented `NGMP_Helpers` with cross-platform C++20 primitives (`<chrono>`, `<thread>`, `<filesystem>`) and storage path handling under `~/.generals_online/`.
- Implemented `NGMP_OnlineServicesManager` singleton with main-thread event queueing for thread-safe UI updates.
- Integrated auth REST calls and JWT token loading in `OnlineServices_Auth.cpp`.
- Integrated `NGMP_OnlineServicesManager` initialization and update ticks in `GameEngine.cpp`.
- Wired lobby refresh hook in `WOLLobbyMenu.cpp`.
- Hardcoded NGMP server host/port and SSL configuration at CMake compile-time (`cmake/ngmp.cmake` and `NGMP_Helpers.cpp`). Stripped all runtime `std::getenv` and local file reads from C++ client to conceal connection details in binary. Added `NGMP_USE_SSL` CMake option with CMake-time `ENV{NGMP_USE_SSL}` fallback.
- Validated clean compilation and linking of `GeneralsXZH` executable on macOS Vulkan target (`macos-vulkan` preset).

### Phase 2: NGMP Code Quality & WebSocket Chat
- Code review of NGMP subsystem. Identified orphan declarations, duplicate code, synchronous blocking HTTP requests, and missing WebSocket implementation.
- Removed orphan `GetServerHTTPPort`/`GetServerSSLPort` declarations from `NGMP_Helpers.h` (functions deleted from `.cpp` in prior phase).
- Created `ngmp_curl_utils.h` with centralised `CurlResponse` + `WriteCallback` to eliminate duplication between `OnlineServices_Auth.cpp` and `OnlineServices_Manager.cpp`.
- Refactored `login()` -> `loginAsync()`: HTTP request now runs on a dedicated `std::thread`, posting `EVENT_AUTH_SUCCESS`/`EVENT_AUTH_FAILURE` events when complete. Added `std::atomic<bool> m_loginInFlight` guard against concurrent requests.
- Refactored `requestLobbyList()` -> `requestLobbyListAsync()`: same pattern. Lobby list written under event mutex before handoff to main thread.
- Implemented WebSocket chat via libcurl native WS API (`CURLOPT_CONNECT_ONLY` + `curl_ws_send`/`curl_ws_recv`): `NGMPChatSession` class manages the persistent connection on a dedicated receive thread; messages dispatched via callback to the NGMP event queue without touching UI.
- Updated `OnlineServices_Init.cpp` to connect `NGMPChatSession` on session restore and join/join all threads on shutdown.
- Added `requestLobbyListAsync()` call to `WOLLobbyMenuInit` under `#if SAGE_USE_NGMP` guard.
- Registered new sources (`OnlineServices_Chat.cpp`, `NGMPChatSession.h`, `ngmp_curl_utils.h`) in `GeneralsMD/Code/GameEngine/CMakeLists.txt`.
- Validated clean build and link of `GeneralsXZH` on `macos-vulkan` preset.
