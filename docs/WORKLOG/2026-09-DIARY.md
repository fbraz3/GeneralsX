# September 2026

> [!NOTE]
> **AI-Generated Content Disclosure**: This worklog is automatically generated and maintained by AI coding agents to document daily progress, debugging sessions, and technical decisions.

## 06/09/2026
### Fix Retina Fullscreen UI Hit Testing
- Traced issue #267 to the SDL mouse transform used for fullscreen, high-density, aspect-fitted output.
- Preserved SDL's fractional window coordinates and now calculate the aspect-fit viewport directly in current logical window space instead of round-tripping a cached physical-pixel viewport through integer coordinates.
- Applied the same correction to Generals and Zero Hour while retaining black-bar clamping and windowed behavior.

## 08/09/2026
### Synchronize TheSuperHackers Upstream
- Merged upstream commits up to `689a09ac4` from `thesuperhackers/main` into sync branch `thesuperhackers-sync-09-08-2026`.
- Resolved 14 conflicted files across build system, rendering, game engine, tools, and documentation.
- Adopted upstream GUI centralization into `Core/GameEngine/` (`GameWindowManager`, `GameWindowTransitionsStyles`, `AnimateWindowManager`) and cleaned up redundant files in `Generals/`.
- Preserved GeneralsX frame performance optimization in `Core/Libraries/Source/WWVegas/WW3D2/ww3d.cpp` (`DX8Wrapper::Set_Transform_Dirty()`) to prevent per-frame render state invalidation overhead (~33KB memset + texture unbinds).
- Reconciled save game loading diagnostics in `GameState.cpp` (both Generals and Zero Hour) combining `std::fprintf(stderr, ...)` release console logging with upstream's `showQueuedSaveGameLoadFailure()` user dialog.
- Maintained strict audio parity across MiniAudio, OpenAL, and Miles by removing deprecated `has3DSensitiveStreamsPlaying()` and dispatching speech volume adjustments to `TheVideoPlayer`.
- Preserved all cross-platform build options in `cmake/config-build.cmake`, pinned dependency versions in `vcpkg-lock.json`, and incorporated upstream's `ffmpeg` manifest feature in `vcpkg.json`.
- Rejected upstream changes to `.github/workflows/` preserving GeneralsX multi-platform CI and replay validation pipelines.
- Successfully configured `macos-vulkan` and built both `z_generals` (Zero Hour) and `g_generals` (Generals) binaries; validated clean startup and subsystem initialization for both games.
- Removed obsolete duplicate `BinkVideoPlayer.h` and `BinkVideoPlayer.cpp` in `GeneralsMD/Code/GameEngineDevice/` that shadowed the canonical `Core` header and caused Windows MSVC builds to fail on abstract `setVolume` instantiation.

### Modularize PR Review and CodeRabbit Prompts
- Modularized `.github/prompts/review-pull-request.prompt.md` into two dedicated workflows:
  - `.github/prompts/review-pull-request.prompt.md`: Focused on architectural coherence, merge safety, platform isolation, determinism, improvement opportunities, edge cases, and 1-commit policy enforcement, with an interactive inquiry allowing the user to trigger CodeRabbit triage.
  - `.github/prompts/resolve-coderabbit-findings.prompt.md`: Dedicated prompt to triage, address, or technically rebut all CodeRabbit review findings via GitHub API without conversational filler, preserving dual-engine parity and deterministic math.

### Cross-Platform CI Build Caching
- Introduced vcpkg binary caching (`actions/cache/restore` and `actions/cache/save`) on Windows MSVC (`x86-windows`) in `.github/workflows/build-windows.yml` to prevent rebuilding dependencies from source and dramatically reduce CI duration.
- Added parity vcpkg binary caching for macOS (`arm64-osx`) in `.github/workflows/build-macos.yml`.
- Configured dedicated cache key hashing on both `vcpkg.json` and `vcpkg-lock.json` alongside fallback prefix restore keys.

## 06/09/2026
### Abort Premature SCUD Storm Launches
- Fixed issue #284 by making the existing missile-launcher null guard active in normal builds and safely checking the special-power lookup.
- Restored the matching escape checks in the base and both OCL special-power paths so a rejected launch cannot continue creating the SCUD Storm effect.
- Limited the sentinel path to buildings that are actually under construction; completed launchers restore their non-serialized module pointer instead of becoming permanently disabled after loading.
- Evaluated under-construction status prior to module cache lookup to reject premature launches in all states.
- Validated each request against the missile launcher's configured power before restoring or rejecting it, so objects with multiple special powers cannot bind the update to an unrelated module.
- Kept the sentinel temporary: the normal build-complete special-power initialization replaces it with the regular recharge frame.

## 13/09/2026
### Transition CI/CD Release Pipeline to Semantic Versioning (SemVer)
- Replaced the manual `GeneralsX-Beta-XX` release tag dispatch form with automated SemVer patch incrementing based on `base-version.txt`.
- Created `base-version.txt` (initialized to `1.0`) at the repository root to control the major/minor release stream.
- Implemented `scripts/tooling/release/calculate_next_version.sh` to dynamically detect existing tags matching `MAJOR.MINOR.*` and calculate the next patch level (`X.Y.0` for new streams, incrementing `+1` from the highest existing tag).
- Added comprehensive unit tests in `scripts/qa/test_calculate_next_version.sh` validating empty repos, numeric patch ordering (e.g. 1.0.9 vs 1.0.10), stream bumps (1.5 -> 1.5.0), legacy tag tolerance, and GitHub Actions output integration.
- Updated `.github/workflows/release.yml` to automatically determine release version, manage concurrency globally, fix Dry Run checkouts, search changelogs across both SemVer and legacy beta tags, and remove beta disclaimers.
- Updated `Core/GameEngine/Source/Common/version.cpp` to format project watermark cleanly for SemVer and preserve backwards compatibility with legacy beta tags.
- Refactored `.github/prompts/prepare-release-draft.prompt.md` and `docs/WORKDIR/support/RELEASE_PIPELINE.md` to document and support the new stable SemVer workflow.

### Clean macOS App Bundle Packaging (.zip)
- Eliminated intermediate `.tar` wrapper on macOS app bundles that caused double-packaged `.tar.zip` release downloads.
- Added `-y` to `zip` commands in `bundle-macos-zh.sh` and `bundle-macos-generals.sh` to preserve internal symlinks directly within `.zip` archives.
- Updated `.github/workflows/build-macos.yml` to upload bundle artifacts as native `.zip`.
- Updated `.github/workflows/replay-tests.yml` to extract `.zip` bundles with seamless fallback to `.tar`.
- Updated `.github/workflows/release.yml` to release `macOS-GeneralsXZH.zip` and `macOS-GeneralsX.zip` directly for clean single-step extraction on macOS.
- Updated installation guide and pipeline support documentation to reflect `.zip` assets.

### Update Visual Identity and Regenerate Windows Executable Icons
- Replaced app icons (`assets/generalsx_icon.png` and `assets/generalsx-zh_icon.png`) with classic military insignia emblems combined with the GeneralsX "X" motif for improved small-size icon readability and authenticity.
- Created automated multi-resolution ICO generation tool `scripts/tooling/generate_windows_icons.py`.
- Regenerated `Generals/Code/Main/Generals.ico` and `GeneralsMD/Code/Main/Generals.ico` across all 7 standard resolutions (16x16, 24x24, 32x32, 48x48, 64x64, 128x128, 256x256) with 32-bit RGBA transparency.

### Remove Obsolete Splash Screen Asset
- Removed `assets/generalsx_splash.png` (3.1MB) which had no code or pipeline references and was left over from older concept mockups.
- Kept the asset folder focused exclusively on retail-aligned military insignia icons.

### Update Authentication Portal Branding Logo
- Updated authentication portal brand assets with the new high-resolution logo (`public/assets/images/logo3.png`).
- Replaced references across all portal templates (`index.php`, `success.php`, `error.php`, `termsofservice.php`, `codeofconduct.php`, `privacypolicy.php`).
- Validated PHP syntax across all templates.
- Pushed commit `68c414a` to `fbraz3/generalsx-login` (`origin/master`).
- Deployed live changes to the production container environment.
- Verified live HTTP/2 200 delivery of `logo3.png` and HTML favicon/branding integration on the authentication portal.

## 14/09/2026
### Sanitize Hardcoded Local Filesystem Paths in Documentation
- Cleaned hardcoded local filesystem paths (personal and user home directories) across worklogs, status reports, and session notes to protect personal environment paths and ensure portability.
- Converted absolute `file:///...` links in `docs/WORKLOG/README.md`, `docs/WORKLOG/2026-06-DIARY.md`, and `docs/WORKDIR/reports/INFANTRY_SHADOW_FIX_SUMMARY.md` to clean relative markdown links.
- Generalized personal user paths in `docs/WORKLOG/2026-02-DIARY.md`, `docs/WORKLOG/2026-03-DIARY.md`, and active/archived work notes (`CURRENT_INVESTIGATION.md`, `2026-02-LESSONS.md`, `FUTURE_CD_REMOVAL_CLEANUP.md`, `SESSION_49_BIG_VFS_DISCOVERY.md`, `SESSION22_HANDOFF.md`, `SESSION_28_STATUS.md`) using `$HOME`, `~`, or relative working directory paths.

### Migrate User Guides and Build Instructions to GitHub Wiki
- Created platform build instructions (`Building-on-Linux`, `Building-on-macOS`) and updated all user-facing tutorials on the project GitHub Wiki (`fbraz3/GeneralsX.wiki`).
- Replaced in-repo files in `docs/BUILD/` and `docs/HOWTO/` with clean redirection notices linking directly to their respective wiki pages.
- Updated cross-references across `README.md`, release workflow, release prompt, pipeline support docs, and developer guidelines (`AGENTS.md`, `docs.instructions.md`) to point to the project wiki.

### Cleanup and Pruning of Obsolete Documents in docs/
- Removed 58 obsolete AI-generated session reports, ephemeral handoff logs from February/March, solved debug spikes, and completed upstream sync conflict notes across `docs/WORKDIR/archive/`, `docs/WORKDIR/reports/`, and `docs/WORKDIR/planning/`.
- Moved floating-point determinism analysis (`generalsx-fp-determinism-lib-analysis.md`) into `docs/WORKDIR/support/FP_DETERMINISM_ANALYSIS.md`, eliminating loose files in `docs/WORKDIR/` root.
- Consolidated `docs/WORKDIR/archive/README.md` and pruned stale references while preserving historical phase planning, foundation architecture, and all monthly diaries.

### Update CodeRabbit Rules and Discontinue 1-Commit Requirement
- Discontinued the legacy 1-commit-per-PR squash requirement in `.coderabbit.yaml` and prompt workflows (`review-pull-request.prompt.md`, `resolve-coderabbit-findings.prompt.md`), allowing PRs with multiple atomic commits following Conventional Commits format.
- Added a strict pre-merge check rule to `.coderabbit.yaml` preventing the introduction of hardcoded local/physical paths (`/Users/...`, `/home/...`) or sensitive internal environment info across code, docs, and configs.

### Refine Release Notes Structure and Disclaimer
- Simplified the top-level issue disclaimer in `.github/workflows/release.yml`, `.github/prompts/prepare-release-draft.prompt.md`, and `docs/WORKDIR/support/RELEASE_PIPELINE.md` to standard non-blockquote text (`If you run into any problems, please [open an issue](https://github.com/fbraz3/GeneralsX/issues) so we can investigate.`).
- Moved the `# Getting Started` section (including installation guidance and community ports) ahead of `# What's New` in both release generation and draft preparation prompts.
- Synced the community ports list across `.github/workflows/release.yml` and `.github/prompts/prepare-release-draft.prompt.md` to include `wasm-generals`.
- Removed inactive community port `GeneralsXWeb` by `meerzulee` across `README.md`, `.github/workflows/release.yml`, and `.github/prompts/prepare-release-draft.prompt.md`.

### Address CodeRabbit Review Findings on PR #301
- Sanitized hardcoded container deployment path in worklog entry.
- Hardened base version validation in `scripts/tooling/release/calculate_next_version.sh` to require exactly two numeric components (`MAJOR.MINOR`), preventing three-part versions like `1.0.0` from being improperly accepted as base streams.
- Isolated GitHub output capture test in `scripts/qa/test_calculate_next_version.sh` by executing inside `$REPO1` and added automated test case 10 validating rejection of 3-part base versions.
- Sanitized authentication portal endpoint URLs from previous worklog entries.
- Updated `.coderabbit.yaml` Conventional Commit Standards check to explicitly ignore standard git merge commits generated when syncing branches.

### Package macOS App Bundles Directly at Zip Root
- Excluded external helper script `run.sh` from `bundle-macos-zh.sh` and `bundle-macos-generals.sh` zip creation, packaging only the `.app` bundle directly so that macOS Archive Utility extracts the `.app` directly without creating an enclosing wrapper folder.
- Updated `.github/workflows/build-macos.yml` to upload the pre-built `.zip` archive containing the `.app` bundle directly at root, preserving symlinks and executable file permissions.
- Updated `.github/workflows/release.yml` to use the pre-built macOS `.zip` archives directly as release assets (`macOS-GeneralsXZH.zip` and `macOS-GeneralsX.zip`), preserving bundle structure, symlinks, and permissions with zero intermediate re-packing.
- Updated `.github/workflows/replay-tests.yml` to extract the `.zip` archive first to retain executable permissions and symlinks.

### Address PR #301 Review Findings by arazmj
- Addressed orphan tag risk on pipeline failures by updating `cleanup-tag-on-failure` in `.github/workflows/release.yml` to depend on all build jobs and `prepare-release`, ensuring tag rollback also executes if `prepare-release` fails or is cancelled (while verifying no release was published).
- Addressed macOS artifact file mode preservation by uploading the native `.zip` generated by `zip -y` instead of raw extracted directory, guaranteeing executable bits and symlinks are retained for replay tests and release assets.

### Disable Double-Zipping on macOS CI Artifacts via `archive: false`
- Configured `archive: false` in `actions/upload-artifact@v7` within `.github/workflows/build-macos.yml`, allowing the pre-compressed `${OUTPUT}-macos-arm64.zip` single file to be uploaded directly without an outer ZIP wrapper.
- Synchronized artifact names across `.github/workflows/ci.yml` (`game-artifact: GeneralsXZH-macos-arm64.zip`) and `.github/workflows/release.yml` (`GeneralsXZH-macos-arm64.zip` / `GeneralsX-macos-arm64.zip`) for seamless consumption with `actions/download-artifact@v8`.

### Disable Artifact Zipping on Linux Flatpak Bundles via `archive: false`
- Configured `archive: false` in `actions/upload-artifact@v7` within `.github/workflows/build-linux-flatpak.yml`, allowing single `.flatpak` files to be uploaded directly without GitHub wrapping them in an outer ZIP archive when downloaded from the Actions UI.
- Renamed the upload artifact name to `${{ matrix.game.output }}-${{ inputs.preset }}.flatpak` (`GeneralsXZH-linux64-deploy.flatpak` and `GeneralsX-linux64-deploy.flatpak`).
- Synchronized artifact download names in `.github/workflows/ci.yml` (`replay-test-linux`) and `.github/workflows/release.yml` (`Download Linux Zero Hour Flatpak artifact` and `Download Linux Generals Flatpak artifact`).

### Configure `skip-decompress: true` for Direct Artifact Downloads
- Traced macOS replay test failure (`Permission denied` on `./GeneralsXZH`) to `actions/download-artifact@v8` auto-decompressing `GeneralsXZH-macos-arm64.zip` via Node.js without preserving POSIX executable permissions.
- Configured `skip-decompress: true` on `actions/download-artifact@v8` in `.github/workflows/replay-tests.yml` so that pre-compressed `.zip` and `.flatpak` bundles are downloaded as exact binary files, allowing Info-ZIP (`unzip -q`) to perform native extraction with mode preservation.
- Added explicit `chmod +x` hardening on the game binary and bin directory within `replay-tests.yml`.
- Configured `skip-decompress: true` on single-file artifact downloads in `.github/workflows/release.yml` for macOS zips and Linux flatpaks.

### Address CodeRabbit and arazmj Review Findings on Tag Cleanup and Flatpak Fallback
- Hardened orphan tag deletion in `.github/workflows/release.yml`:
  - Queried `repos/:owner/:repo/releases/tags/:tag` via GitHub API to verify release existence by HTTP status, deleting tags only on confirmed 404 and refusing tag mutation on API query errors (5xx/403/timeout).
  - Used `git ls-remote --exit-code --tags origin` to distinguish confirmed tag absence (exit 2) from query errors, and eliminated error suppression to ensure remote tag deletion failures propagate as job failures.
- Preserved canonical Flatpak artifact filename in `.github/workflows/build-linux-flatpak.yml`:
  - When the dynamic candidate search locates a Flatpak bundle whose filename differs from `${EXPECTED}`, renamed the candidate to `${EXPECTED}` before writing to `steps.bundle.outputs.path`, ensuring `archive: false` uploads match the exact artifact name expected by downstream consumers.
