# September 2026

> [!NOTE]
> **AI-Generated Content Disclosure**: This worklog is automatically generated and maintained by AI coding agents to document daily progress, debugging sessions, and technical decisions.

## 01/09/2026
### 01/09/2026 - Fix Generals Base Game Compilation
- **Context**: Fixed compilation error in `Generals` base game (`fatal error: 'GameNetwork/GeneralsOnline/NGMPGame.h' file not found`).
- **Root Cause**: The `NGMPGame.h` header and `TheNGMPGame` global pointer belong to the expansion layer (`GeneralsMD`), while NGMP integration is currently restricted to Zero Hour.
- **Fix**: Removed the `TheNGMPGame` include and code block from `Generals/Code/GameEngine/Source/GameLogic/System/GameLogic.cpp`.
- **Validation**: Both builds (`build-macos-generals.sh` and `build-macos-zh.sh`) completed successfully (Exit 0).

### 01/09/2026 - QuickMatch via NGMP Integration
- **Context**: Implementation of ranked matchmaking (QuickMatch) via NGMP matching parity with the reference repository.
- **Changes Implemented**:
  1. `OnlineServices_Manager.h/cpp`:
     - Added `widenMatchmakingAsync()` method for the `POST /Matchmaking/Widen` endpoint.
     - Updated `startMatchmakingAsync()` with the complete payload (`exe_crc`, `ini_crc`, `anticheat_id`) expected by the C# controller.
     - Added `EVENT_MATCHMAKING_MATCH_FOUND` and `EVENT_MATCHMAKING_MESSAGE` events.
     - Implemented WebSocket handlers for `msg_id: 20` (`MATCHMAKING_ACTION_JOIN_PREARRANGED_LOBBY`), `msg_id: 21` (`MATCHMAKING_ACTION_START_GAME`), and `msg_id: 22` (`MATCHMAKING_MESSAGE`).
  2. `WOLQuickMatchMenu.cpp`:
     - Integrated asynchronous loading of playlists and maps via `requestPlaylistsAsync()` and `getPlaylists()`.
     - Connected `buttonStartID`, `buttonStopID`, and `buttonWidenID` handlers to NGMP APIs with `MinSelectedMaps` validation.
     - Dispatched matchmaking WebSocket events directly to the UI (chat status messages, match found sound, and simultaneous game launch).

### 01/09/2026 - QuickMatch Lobby Join & Shutdown Fixes
- **Context**: Investigation and resolution of match found timeouts in QuickMatch and crashes when quitting the game via Cmd+Q.
- **Root Causes & Solutions**:
  1. `Playlists 401 Unauthorized`: Added `Authorization: Bearer` header and a robust parser supporting indexed playlist dictionaries.
  2. `Join Lobby HTTP 417 Expectation Failed`: `joinLobbyAsync` was sending `"anticheat_id": -1`, which differed from `AnticheatID = 0` (`NONE`) of the lobby created by the matchmaking server. Fixed to send `0`.
  3. `Matchmaking Start HTTP 204`: Accepted status 204 (No Content) as a successful response in `startMatchmakingAsync`.
  4. `Segfault on Cmd+Q Shutdown`: Added null pointer checks for `TheGameSpyInfo`, `userData`, `TheShell`, and `TheTransitionHandler` in `WOLQuickMatchMenuShutdown`, `WOLLobbyMenuShutdown`, and `WOLGameSetupMenuShutdown`.

### 01/09/2026 - MapCache Slashes Normalization & Matchmaking Fixes
- **Context**: Investigation of map resolution failure for matchmaking maps (e.g. `Koujou Okawa v3\Koujou Okawa v3.map`).
- **Root Cause**: The matchmaking server sends map paths with Windows-style backslashes (`\`), whereas macOS and POSIX filesystems index using forward slashes (`/`). `MapCache::findMap` did not properly normalize path comparators in `ends_with`, resulting in `nullptr` (map not found).
- **Solution**:
  1. Refactored `MapCache::findMap` in `MapUtil.cpp` to unify slash normalization (`\` to `/`), case insensitivity, and base leaf matching.
  2. Maps requested with Windows paths, absolute Unix paths, or display names are now resolved with 100% accuracy.

### 01/09/2026 - UserPreferences Backslash Path Fix & POSIX Normalization
- **Context**: Preference files (`GeneralsOnline\*.ini`) and folders (`GeneralsOnline\Ladders`) were being created with literal backslashes in the root user data directory rather than inside the `GeneralsOnline/` subfolder.
- **Root Cause**: `UserPreferences` invoked `fopen` directly with Windows-formatted paths (`GeneralsOnline\\*.ini`), and `PeerDefs.cpp`/`LadderDefs.cpp` created directories with literal backslashes that POSIX kernels treat as valid filename characters rather than path separators.
- **Solution**:
  1. `UserPreferences::load()` and `UserPreferences::write()` now normalize backslashes (`\`) to forward slashes (`/`) and ensure parent directory creation (`std::filesystem::create_directories`) before saving any `.ini`.
  2. Updated all hardcoded `GeneralsOnline\\` paths to `GeneralsOnline/` across `UserPreferences.cpp`, `PeerDefs.cpp`, `LadderDefs.cpp`, `PersistentStorageThread.cpp`, `MainMenuUtils.cpp`, `PopupHostGame.cpp`, and `PopupPlayerInfo.cpp` (with complete backport to base Generals).
- **Validation**: Zero Hour build completed successfully (Exit 0) and deployed to `~/GeneralsX/GeneralsZH`.

### 01/09/2026 - Multi-Directory User Maps Discovery & Disk Fallback
- **Context**: Investigation of transfer/not-found errors for ranked maps selected by QuickMatch (e.g. `[RANK] Vendetta ZH v1`).
- **Root Cause**: On macOS, `TheGlobalData->getPath_UserData()` points to `~/Library/Application Support/GeneralsX/GeneralsZH/`, and `MapCache` only indexed the `Maps` subfolder in that specific directory. All 40 maps from the NGMP package were placed in the user's legacy documents folder (`~/Documents/Command and Conquer Generals Zero Hour Data/Maps/`).
- **Solution**:
  1. `MapCache::updateCache()` now automatically discovers and indexes maps in `~/Documents/Command and Conquer Generals Zero Hour Data/Maps/` as well as cloud sync paths.
  2. Implemented a direct-disk fallback search in `MapCache::findMap()`: if a map is not loaded in memory at call time, the engine scans for the `.map` file on disk, dynamically registers it into the cache, and returns its metadata.
  3. Synchronized all custom maps into `~/Library/Application Support/GeneralsX/GeneralsZH/Maps/`.
  4. Kept the `Back` button enabled during the match-found state in the QuickMatch menu.
- **Validation**: Zero Hour build compiled with Exit 0 and deployed to `~/GeneralsX/GeneralsZH`.

### 01/09/2026 - UserPreferences Filename Preservation & Country Selection Persistence
- **Context**: The country/region selection popup (`GSOVERLAY_LOCALESELECT`) appeared repeatedly on every login to the online menu, even with `Locale = 27` saved in `GSMiscPref*.ini`.
- **Root Cause**:
  1. In `UserPreferences::load()`, invoking `AsciiString::getBufferForRead()` for path normalization on `normalizedFname` cleared the string buffer to empty (`""`), causing the engine to attempt opening the directory rather than the `.ini` file.
  2. In `GlobalData.cpp` on POSIX environments, `BuildUserDataPathFromRegistry()` returned the path without a trailing forward slash `/`, causing malformed path concatenations.
- **Solution**:
  1. `UserPreferences::load()` now uses `std::string` for slash normalization and validation of `TheGlobalData->getPath_UserData()`, preserving 100% of filenames (`GSMiscPref*.ini`, `Options.ini`, `CustomPref*.ini`, `QMPref*.ini`).
  2. `GlobalData::BuildUserDataPathFromRegistry()` (Zero Hour and base Generals) guarantees that `getPath_UserData()` always ends with `/` on macOS and Linux.
  3. Updated path concatenations for `map.ini` and `solo.ini` in `GameLogic.cpp` to use portable `/` separators.
- **Validation**: Runtime execution logs confirmed successful loading of `GSMiscPref1.ini`, persistence of `Locale = 27`, and suppression of the redundant country selection dialog.

## 03/09/2026
### 03/09/2026 - NGMP Environment Detection, Browser OAuth Login & Web Portal Customization
- **Context**: Transition support for the production server (`generalsonline.braz.cloud`), separating local development (dev) and production (prod) behaviors, restoring browser-based authentication, and supporting customized web portal URLs.
- **Implementations**:
  1. **Environment Detection (`NGMP::IsDevelopment()`)**:
     - Checks if `NGMP_DEFAULT_HOST` belongs to local/private networks (`192.168.*`, `10.*`, `172.16-31.*`, `localhost`, `127.0.0.1`).
     - In development environments, sets the route to `/env/dev/contract/1/`, retains the bypass with `ILOVECODE`, and disables `UpdateChecker` during startup for faster iteration cycles.
     - In production environments, routes to `/env/live/contract/1/` and enables the full authentication pipeline.
  2. **UUIDv4 Gamecode Generation & Browser Launch**:
     - In production, generates unique tokens in UUIDv4 format (`NGMP::GenerateGamecode()`), opens the default operating system browser via `SDL_OpenURL()`, and displays a non-blocking in-game dialog (`GSMessageBoxCancel`) with clean cancellation support.
  3. **Dynamic Web Portal Configuration (`NGMP_WEB_PORTAL_URL`)**:
     - Added support via CMake cache and environment variables (`cmake/ngmp.cmake` and `scripts/build/linux/build-linux-flatpak.sh`) with conditional compilation `-DNGMP_WEB_PORTAL_URL`.
     - Helpers `GetWebPortalURL()`, `GetBrowserLoginURL()`, and `GetMatchViewURL()` decouple the web portal from the REST/WS backend and update post-game match links (`ScoreScreen.cpp`).
  4. **Mandatory Update Check in Production**:
     - In production, clicking the Online button on the main menu validates client version against `UpdateChecker::hasUpdate()`. If a newer version is available, online access is blocked, displaying a notice requiring an update before joining.
  5. **Discord Authentication Portal (`generalslogin.braz.cloud`)**:
     - Developed web portal in PHP + Nginx (`fbraz3/php-nginx:8.4`) with GeneralsX themed styling, Discord OAuth2 integration, Code of Conduct, and Privacy Policy.
     - Deployed to `web04-ctb.braz.cloud` under `/data/docker/generalslogin/`, linked to the internal network `generalsonline_generals_internal`.
     - Configured reverse proxy on host Nginx listening on port 80 with Cloudflare header preservation (`CF-Connecting-IP`).
     - Configured `webserver_key` in `appsettings.json` on the C# GameServer for `LoginCode` authorization.
  6. **Dynamic Message of the Day (MOTD) in Online Lobby**:
     - Served statically by the web portal at `generalslogin.braz.cloud/motd.txt` with Cloudflare edge caching and `Cache-Control: public, max-age=300`.
     - C++ client (`NGMP_Helpers.cpp`, `OnlineServices_Auth.cpp`, `WOLWelcomeMenu.cpp`) fetches the MOTD in background via `libcurl` (`NGMP::FetchMOTD()`), with customizable URL via `NGMP_MOTD_URL` environment variable (falling back to `${NGMP_WEB_PORTAL_URL}/motd.txt`).
     - Populates `TheGameSpyInfo->setMOTD(...)`, preserving native per-line color tags (`\AARRGGBB`) rendered in the welcome menu info listbox (`WOLWelcomeMenu`).
     - Message configured welcoming players to the public beta, linking to the Code of Conduct, Privacy Policy, and GitHub issue tracker.
  7. **GitHub Actions CI Secrets & Build Script Propagation**:
     - Updated all CI build workflows (`build-macos.yml`, `build-linux.yml`, `build-linux-flatpak.yml`, `build-windows.yml`) to pass `NGMP_WEB_PORTAL_URL` and `NGMP_MOTD_URL` repository secrets to CMake.
     - Updated `cmake/ngmp.cmake`, `scripts/build/linux/build-linux-flatpak.sh`, and `NGMP_Helpers.cpp` to support compile-time definition of `NGMP_MOTD_URL`.
  8. **Cross-Platform Compilation Fixes for Linux (GCC 15) & Windows (MSVC)**:
     - In `NGMP_Helpers.cpp`, reordered standard C++ library headers (`<chrono>`, `<random>`, `<format>`) before legacy game engine headers (`PeerDefs.h`) and explicitly `#undef min` / `#undef max` to resolve GCC 15 macro collision (`error: macro 'min' requires 2 arguments`).
     - Replaced `std::snprintf` with standard C++20 `std::format` in `GenerateGamecode()` to prevent MSVC `_snprintf` macro collision (`error C2039: '_snprintf': is not a member of 'std'`).
   9. **Server-Side LoginCode Handshake & Main Menu Recovery**:
      - Resolved HTTP 404 / 403 login failure where client-generated UUID gamecodes were rejected by C# GameServer in-memory pending logins table (`PendingLoginManager`).
      - Implemented `RequestLoginCodeFromServer()` in `OnlineServices_Auth.cpp` to call `GET /LoginCode` on the GameServer prior to opening the browser. The server issues and registers an official code (`PendingLoginManager.AddPendingLogin`), ensuring both the web portal approval (`POST /LoginCode`) and client polling (`POST /CheckLogin`) find the entry in state `Waiting`.
      - Updated `MainMenu.cpp` to automatically release button and transition locks (`buttonPushed = FALSE`, `dontAllowTransitions = FALSE`) when browser authentication is no longer pending (cancelled or failed), allowing players to re-click Multiplayer without restarting the game.
   10. **CodeRabbit PR #260 Review Comments Resolution**:
      - Addressed 16 CodeRabbit review findings on PR #260 (`feat/generals-online-ngmp`):
        - Guarded `#include <filesystem>` with `#ifndef _WIN32` in `UserPreferences.cpp` for legacy VC6 compatibility.
        - Implemented negative lookup caching (`s_failedMapLookups`) in `MapUtil.cpp` to prevent disk thrashing on missing maps, cleared cache on reload, and removed hardcoded dev path.
        - Scoped `Source/GameNetwork/GeneralsOnline/*.cpp` under `if(SAGE_USE_NGMP)` in `GeneralsMD/Code/GameEngine/CMakeLists.txt`.
        - Preserved `-1` unassigned starting position in `updateLobbyAISide` (`OnlineServices_Manager.cpp`).
        - Moved update checker validation ahead of transition reversal in `MainMenu.cpp`.
        - Added null pointer guard for `TheNGMPGame` before positioning start spots in `WOLGameSetupMenu.cpp`.
        - Refreshed NGMP game list when room filter selection changes in `WOLLobbyMenu.cpp`.
        - Filtered unavailable playlist maps in `WOLQuickMatchMenu.cpp`, stored original map indices in listbox data, and reset slot availability / pointer cleanup on game start.
        - Hardened `NetworkMesh.cpp` with null guards on `identityPeer.GetGenericString()`.
        - Dropped unmapped peer packets in `NextGenTransport.cpp` to prevent routing foreign traffic into slot 0.
        - Added curl timeout options and asynchronous thread execution to `NGMP::FetchMOTD()` in `WOLWelcomeMenu.cpp` to avoid blocking UI initialization.
        - Upgraded fallback `GenerateGamecode()` in `NGMP_Helpers.cpp` to use OS-native CSPRNG (`arc4random_buf`, `/dev/urandom`, `CryptGenRandom`).
        - Moved `m_currentLobbyId` assignment inside HTTP 200/201 success handling in `joinLobbyAsync`.
        - Fixed host migration handling in `OnlineServices_Manager.cpp` so clients remain connected when room ownership migrates.
   11. **CodeRabbit PR #260 Follow-up Hardening & Thread Safety**:
      - Addressed 7 follow-up review findings and resolved all open review threads:
        - Added required GeneralsX inline annotations in `MapUtil.cpp` and `UserPreferences.cpp`.
        - Fixed `WOLQuickMatchMenu.cpp` so column 1 item data continues to hold `const MapMetaData*` to avoid pointer corruption in `saveQuickMatchOptions()`, mapping playlist row indices via `s_qmRowToPlaylistMapIndex`.
        - Required valid lobby and game pointers before launching quick match on `EVENT_GAME_START`.
        - Verified complete read state in `GenerateGamecode()` for both `CryptGenRandom` (Windows) and `/dev/urandom` (Unix) with PRNG fallback.
        - Routed background MOTD (`EVENT_MOTD_FETCHED`) and outcome commit (`EVENT_OUTCOME_COMMITTED`) completions through `NGMPEvent` to main thread dispatch in `OnlineServices_Manager.cpp`, eliminating cross-thread access to `TheGameSpyInfo` and `TheNGMPGame`.
        - Added concurrent request coalescing to `FetchMOTD()` using atomic check.
- **Validation**: Zero Hour (`GeneralsXZH`) and base game (`GeneralsX`) macOS builds compiled cleanly with CMake (Exit 0). HTTP/2 requests via Cloudflare to `https://generalslogin.braz.cloud/` verified with HTTP 200 and cache hit status. LoginCode endpoint verified returning 200 OK with server-issued token. All CodeRabbit review threads resolved on GitHub.


