node_modules/
dist/
.wrangler/
*.tsbuildinfo
coverage/
.DS_Store
*.log
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Deployment credentials are provided through the environment, never a file.
# These patterns exist so a local convenience file can never be committed.
.dev.vars
.dev.vars.*
.env
.env.*
*.pem
