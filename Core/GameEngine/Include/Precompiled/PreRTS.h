#pragma once

// Minimal PreRTS.h for Core library compilation
// This provides just enough for ObjectStatusTypes.cpp and other core stubs to compile
// without pulling in the full game engine dependencies

#include "Lib/BaseType.h"

// Standard includes that ObjectStatusTypes.cpp needs
#include <stddef.h>
#include <cstring>

