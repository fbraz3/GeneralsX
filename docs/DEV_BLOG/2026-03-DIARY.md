# Development Diary - March 2026

---

## 2026-03-02 - Session 58: win64-modern LNK2019 Linker Errors Resolved — generalszh.exe Links

**TL;DR**: Fixed all remaining linker errors (LNK2019, LNK1181, CMake config errors) blocking `generalszh.exe` on the win64-modern stack. Executable now links: 10.4 MB at `build/win64-modern/GeneralsMD/generalszh.exe`.

### Background

Previous session had applied 7 source fixes (SDL3Mouse, SDL3Keyboard, SDL3Main, WWVegas
CMakeLists, Core WW3D2 CMakeLists). This session continued from that point.

### Issues Found & Fixed

**Issue 1 — GENERATOR EXPRESSION: FramGrab.cpp INTERFACE propagation (absolute path)**

`Core/WW3D2/CMakeLists.txt` used `$<$<BOOL:${WIN32}>:FramGrab.cpp>` (relative path) in
an INTERFACE library's `target_sources`. CMake 4.x cannot resolve generator expression
paths to absolute, so the path propagated to GeneralsMD/Generals WW3D2 consumers. Fixed
by using `${CMAKE_CURRENT_SOURCE_DIR}/FramGrab.cpp`.

**Issue 2 — z_wwdownload STATIC empty (vcpkg rejects empty static libs)**

WWDownload directory's `CMakeLists.txt` created `z_wwdownload STATIC` with no sources
(only linking `corei_wwdownload PRIVATE`). vcpkg's `_add_library` override on Windows
rejects empty STATIC libraries. Initial attempt using INTERFACE chaining caused
`corei_wwdownload.lib` to appear in the link command (INTERFACE libs have no .lib). Final
fix: list Core WWDownload `.cpp` files explicitly in `target_sources` using absolute
`${CMAKE_SOURCE_DIR}/...` paths. NOTE: `PROJECT_SOURCE_DIR` in nested CMakeLists points
to the subproject root (GeneralsMD), NOT the workspace root — use `CMAKE_SOURCE_DIR`.

**Issue 3 — FramGrab.cpp: VFW stub body needed for non-ENABLE_FRAME_GRABBER builds**

`framgrab.h` provides a minimal `AVISTREAMINFO` stub when `ENABLE_FRAME_GRABBER` is not
defined. But `FramGrab.cpp` unconditionally used the full VFW API (AVIFileInit, etc.).
Fixed by guarding the full VFW implementation with `#if defined(ENABLE_FRAME_GRABBER) &&
!defined(DISABLE_VFW)` and providing minimal stub constructors/destructors in the `#else`
branch. The stub satisfies the linker; actual AVI recording is disabled until Phase 3.

**Issue 4 — SDL3Mouse.cpp C4189 (unused variable treated as error)**

`frame_offset` declared but never used in the Windows code path. Removed the dead variable.

**Issue 5 — WWDownload POSIX functions missing on MSVC**

`FTP.cpp` and `Download.cpp` use POSIX functions not in MSVC CRT:
- `strlcpy` / `strlcat` — not in Windows SDK; polyfills added in new `wwdownload_compat.h`
- `socklen_t` — not in winsock.h (v1); switch to `winsock2.h` + `ws2tcpip.h` (defines `socklen_t`)
- `ARRAY_SIZE` — not defined in these TUs; added to `wwdownload_compat.h`
- `_S_IWRITE` / `_S_IREAD` — only in Linux path; added `<sys/stat.h>` in Windows block
- `time()` — missing `<time.h>` include in `FTP.cpp` Windows block; added
- `timeGetTime()` — `<mmsystem.h>` with `WIN32_LEAN_AND_MEAN` doesn't guarantee the function; must `#include <timeapi.h>` directly

**Issue 6 — ftp.h / FTP.cpp used winsock.h (Winsock v1) — project uses ws2_32.lib**

Changed `ftp.h` to include `<winsock2.h>` instead of `<winsock.h>`. Winsock2 must be
included BEFORE `windows.h` to avoid the inclusion-order conflict. `ftp.h` is included
before the platform block in both `FTP.cpp` and `Download.cpp`, ensuring correct ordering.

### Files Changed

```
Core/Libraries/Source/WWVegas/WW3D2/CMakeLists.txt                          - Absolute path in generator expression
Core/Libraries/Source/WWVegas/WW3D2/FramGrab.cpp                            - ENABLE_FRAME_GRABBER VFW guard pattern
Core/Libraries/Source/WWVegas/WWDownload/ftp.h                              - winsock.h → winsock2.h
Core/Libraries/Source/WWVegas/WWDownload/FTP.cpp                            - Added missing Windows includes
Core/Libraries/Source/WWVegas/WWDownload/Download.cpp                       - Added windows.h + timeapi.h
Core/Libraries/Source/WWVegas/WWDownload/wwdownload_compat.h                - NEW: strlcpy/strlcat/ARRAY_SIZE/socklen_t
GeneralsMD/Code/Libraries/Source/WWVegas/WWDownload/CMakeLists.txt         - Explicit absolute paths to Core sources
GeneralsMD/Code/GameEngineDevice/Source/SDL3Device/GameClient/SDL3Mouse.cpp - Removed dead frame_offset variable
docs/WORKDIR/lessons/2026-03-LESSONS.md                                    - 6 new lessons documented
```

### Result

```
[10/11] Linking CXX executable GeneralsMD\generalszh.exe
EXIT: 0
build/win64-modern/GeneralsMD/generalszh.exe  10,393,088 bytes  2026-03-02
```

### Next Steps

- Deploy game data and run `generalszh.exe -win -noshellmap` for smoke test
- If crashes on startup, attach debugger and fix runtime errors

---

## 2026-03-01 - Session 57: Phase 6 win64-modern Audit + Three Bug Fixes

**TL;DR**: Audited all Phase 6 (win64-modern) changes for band-aids and cross-platform regressions. Found and fixed 3 real bugs. No gameplay or Linux build logic was broken.

### Audit Scope

Reviewed all uncommitted Phase 6 diffs: compat headers (`gdi_compat.h`, `string_compat.h`,
`types_compat.h`, `file_compat.h`, `windows_compat.h`, `windows.h`, `windows_wrapper.h`),
CMakeLists changes (`z_gameengine`, `CompatLib`), build scripts, `StackDump.cpp`, `File.h`.

### Bugs Found & Fixed

**Bug 1 — CRITICAL: `gdi_compat.h` content duplicated outside `#ifndef _WIN32` guard**

The Phase 6 patch wrapped the GDI stubs with `#ifndef _WIN32` / `#endif` but forgot to
remove the original un-guarded copy below. The file had 438 lines with lines 229–438 being
an exact duplicate of lines 8–228, but outside any guard. Fixed by truncating to line 225
(ending at `#endif // !_WIN32`). 4 lessons documented in `docs/WORKDIR/lessons/2026-03-LESSONS.md`.

**Bug 2 — PERFORMANCE: `target_precompile_headers(z_gameengine …)` silently removed**

The call was deleted from `GameEngine/CMakeLists.txt` without explanation. `PreRTS.h` is
safe to use as PCH on all platforms (already has `#ifdef _WIN32` guards for SDK vs compat
shim). Restored. Expected compile-time improvement: 2–4× for `z_gameengine` full rebuilds.

**Bug 3 — LOGIC: `windows_compat.h` used CMake option as C++ preprocessor guard**

`#if !defined(_WIN32) || (defined(_WIN32) && !defined(SAGE_USE_DX8))` — `SAGE_USE_DX8` is
a CMake `option()`, never a `-D` define. The condition was always `true` (guard always applied).
Additionally, `windows_compat.h` is Linux-only (shim gate prevents it reaching Windows at all).
Fixed: replaced with unconditional defines and a clear comment.

### Files Changed

```
GeneralsMD/Code/CompatLib/Include/gdi_compat.h        - Removed duplicate block (225 lines, was 438)
GeneralsMD/Code/GameEngine/CMakeLists.txt             - Restored target_precompile_headers
GeneralsMD/Code/CompatLib/Include/windows_compat.h   - Removed bogus SAGE_USE_DX8 guard
docs/WORKDIR/lessons/2026-03-LESSONS.md               - 4 new lessons created
```

### Cross-Platform Impact Assessment

| Platform | Impact |
|----------|--------|
| Linux (linux64-deploy) | Fix 1 removes latent ODR violations; no functional change. Fix 2 faster builds. Fix 3 no change (file unreachable on Windows, correct behavior unchanged on Linux). |
| Windows Modern (win64-modern) | Fix 2 faster builds. Fix 3 guard was always true anyway, so no behavioral change. |
| Windows Legacy (vc6/win32) | No changes to those code paths. |

### Phase 6 Overall Assessment

The Phase 6 (win64-modern) work is architecturally sound. The main strategy choices are correct:
- `CompatLib/Include` excluded from Windows include paths (prevents shim shadowing SDK)
- `#ifndef _WIN32` guards on Linux-only compat stubs
- 64-bit DbgHelp API (`StackWalk64`, `STACKFRAME64`) for x64 stack traces
- GLI upgrade to master commit for GLM 1.0.3 compatibility
- `gli.cmake` in-tree patch for ADL ambiguity fix

The three bugs found were all in the compat header layer (not in game logic or the graphics
pipeline) and none broke Linux correctness. All fixes are minimal, targeted, and documented.

