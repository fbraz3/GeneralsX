# CHANGES/2026-05.md

## Summary

May 2026 changes summary — a chronological record of all wiki-relevant changes.

---

## Overview

This month has focused on:
1. Platform layer hardening (SDL3, DXVK, OpenAL)
2. Retail compatibility fixes (FPS caps, case-sensitive assets, replay paths)
3. CI/CD improvements (macOS migration, Flatpak integration)
4. Upstream sync with thesuperhackers

---

## Detailed Changes

### 2026-05-18

#### CI and Deployment
- Migrated CI to keep only macOS and Linux Flatpak builds
- Switched Linux headless replay tests to run inside Flatpak
- Removed Linux gzip and Linux AppImage jobs from `.github/workflows/ci.yml`

**Wiki Updates:**
- Updated `ENTITIES/PLATFORM.md` (FFmpeg packaging section)
- Updated `TOPICS/BUILD.md` (Docker/Flatpak workflow)
- Updated `TOPICS/TESTING.md` (headless replay in Flatpak)

**Related:**
- Dev blog: 2026-05-18 CI migration to macOS + Flatpak

---

### 2026-05-17

#### Replay Testing
- Restored replay pass/fail reports in CI (Linux + macOS)
- Per-file pass/fail details for both platforms
- Explicit CI failure behavior when at least one replay fails

**Wiki Updates:**
- Updated `TOPICS/TESTING.md` (CI/CD testing section)
- Updated `CONCEPTS/RETAIL.md` (replay testing protocol)

#### Replay Bootstrap
- Updated CLI parsing to accept both single-hyphen and GNU-style double-hyphen flags (`-headless`/`--headless`, `-replay`/`--replay`)
- Added Linux SDL3 entrypoint guard to skip SDL video/Vulkan/window bootstrap when headless mode is active
- Fixed `SDL3GameEngine::init()` to support headless execution without SDL window handles

**Wiki Updates:**
- Updated `TOPICS/DEBUGGING.md` (command-line parameters section)
- Updated `CONCEPTS/RETAIL.md` (replay testing checklist)

#### Headless Mode SDL3
- Updated Linux replay test workflow to force SDL dummy backends in headless mode

**Wiki Updates:**
- Updated `CONCEPTS/RETAIL.md` (headless testing requirements)

---

### 2026-05-11

#### FPS Uncapped Fix (ISSUE #132)

**Changes:**
1. **FramePacer API guard:** Added `#ifdef _WIN32` guards for `timeBeginPeriod()`/`timeEndPeriod()` in `FramePacer.cpp`
2. **FPS limit validation:** Added early-exit handling for invalid FPS targets in `FrameRateLimit::wait()`
3. **FPS init logic fix:** Changed condition from `if (m_useFpsLimit && m_framesPerSecondLimit <= 0)` to `if (m_framesPerSecondLimit <= 0)` to always ensure valid BaseFps (30) default
4. **Build flag:** Added `-ffp-contract=off` for non-MSVC builds

**Wiki Updates:**
- Created `CONCEPTS/DETERMINISM.md` (FPS caps, math consistency)
- Updated `ENTITIES/PLATFORM.md` (FFmpeg section)
- Updated `CONCEPTS/RETAIL.md` (determinism rules)

**Related:**
- Dev blog: 2026-05-11 Fix FPS uncapped and frame counter instability

#### Linux Case-Sensitive Assets (ISSUE #128)

**Changes:**
- Fixed `StdLocalFileSystem` asset-root fallback path resolution on Linux to perform case-insensitive traversal
- Kept change Linux-scoped (`#ifdef __linux__`) to avoid changing macOS behavior

**Wiki Updates:**
- Updated `ENTITIES/PLATFORM.md` (SDL3 known issues)
- Updated `ARCHITECTURE/DATA.md` (VFS known issues)
- Updated `CONCEPTS/RETAIL.md` (asset handling)

**Related:**
- Dev blog: 2026-05-11 Fix Linux case-sensitive asset lookup

---

### 2026-05-10

#### macOS Transparency Regression (ISSUE #131)

**Changes:**
- Routed pillarbox render-target switching through `DX8Wrapper::Set_Render_Target(...)`
- Added post-blit `Invalidate_Cached_Render_States()` in `DX8Wrapper::End_Scene()`
- Ensured pillarbox presentation does not leave `IsRenderToTexture` semantically enabled

**Wiki Updates:**
- Updated `CONCEPTS/RETAIL.md` (macOS platform issues)
- Updated `CONCEPTS/PORTING.md` (platform issues section)

**Related:**
- Dev blog: 2026-05-10 Fix macOS transparency regression

---

### 2026-05-08

#### GitHub Actions Upgrade

**Changes:**
- Upgraded to Node 24 majors for active release and build workflows
- Updated workflows to use Node 24 action versions: `actions/checkout@v6`, `actions/cache@v5`, `actions/upload-artifact@v7`, `actions/download-artifact@v8`

**Wiki Updates:**
- Updated `TOPICS/BUILD.md` (CI/CD section)

---

### 2026-05-07

#### Upstream Sync with thesuperhackers

**Changes:**
- Performed full upstream merge from `thesuperhackers/main`
- Created sync branch `thesuperhackers-sync-05-07-2026`
- Resolved all conflicts preserving GeneralsX cross-platform direction
- Handled unify moves into `Core/` for shared gadget paths
- Resolved modify/delete conflicts in legacy `Generals` gadget files

**Wiki Updates:**
- Updated `ENTITIES/REFERENCES.md` (thesuperhackers section)
- Updated `TOPICS/BRANCHING.md` (sync history)

**Related:**
- Dev blog: 2026-05-07 Full TheSuperHackers upstream sync

---

### 2026-05-05

#### POSIX MapCache Path and Replay Map-Name Portability

**Changes:**
- Fixed `MapCache.ini` path join in map cache read/write code to use host-portable separators
- Added replay CRC mismatch stderr diagnostics
- Added percent encode/decode for replay map field serialization/parsing

**Wiki Updates:**
- Updated `ARCHITECTURE/DATA.md` (INI configuration, replay system)
- Updated `CONCEPTS/RETAIL.md` (replay compatibility)

**Related:**
- Dev blog: 2026-05-05 Fix POSIX MapCache path and replay map-name portability

---

### 2026-05-04

#### Headless Replay Texture Path

**Changes:**
- Added null guards in `D3DXCreateTexture` entrypoint for unavailable DX8 device/output
- Hardened `DX8Wrapper::_Create_DX8_Texture` to return safely when no DX8 device available
- Updated texture load tasks to fail gracefully when `_Create_DX8_Texture` returns null

**Wiki Updates:**
- Updated `CONCEPTS/PORTING.md` (platform issues)
- Updated `ENTITIES/PLATFORM.md` (DXVK section)

**Related:**
- Dev blog: 2026-05-04 Headless replay texture path stabilized

---

### 2026-05-03

#### Deterministic Math Phase 1-4

**Changes:**
- Added `-ffp-contract=off` for non-MSVC builds
- Introduced shared `Sqrt(double)` gateway in `trig.h`
- Replaced core geometry `sqrt` call sites in `BaseType.h`
- Routed `Trig.cpp` through WWMath wrapper functions
- Added `cmake/gamemath.cmake` as Phase 4 deterministic math integration scaffold

**Wiki Updates:**
- Updated `CONCEPTS/DETERMINISM.md` (math consistency section)
- Updated `ENTITIES/CORE.md` (build flags)

**Related:**
- Dev blog: 2026-05-03 Deterministic math Phase 1-4 baseline

---

### 2026-05-03

#### macOS Build Scripts Hardening

**Changes:**
- Added `resolve_vcpkg_root()` to both macOS build entry scripts
- Validated `VCPKG_ROOT` when present
- Added explicit actionable error guidance when vcpkg cannot be found

**Wiki Updates:**
- Updated `TOPICS/BUILD.md` (macOS build prerequisites)

**Related:**
- Dev blog: 2026-05-03 Harden macOS build scripts for missing VCPKG_ROOT

---

## Summary Statistics

| Category | Count |
|----------|-------|
| New wiki pages | 4 |
| Updated wiki pages | 15 |
| Platform changes | SDL3, DXVK, OpenAL |
| Retail fixes | FPS caps, case-sensitive assets, replay paths |
| CI improvements | macOS migration, Flatpak integration |

---

## See Also

- [WIKI_PAGES/README.md](./README.md) — Wiki index
- [CONCEPTS/DETERMINISM.md](../CONCEPTS/DETERMINISM.md) — Determinism principles
- [CONCEPTS/RETAIL.md](../CONCEPTS/RETAIL.md) — Retail compatibility
- [TOPICS/BUILD.md](../TOPICS/BUILD.md) — Build instructions

---

**Last updated**: 2026-05-18 | **Sources**: Dev blog, commit history
