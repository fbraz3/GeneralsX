# September 2026

> [!NOTE]
> **AI-Generated Content Disclosure**: This worklog is automatically generated and maintained by AI coding agents to document daily progress, debugging sessions, and technical decisions.

## 01/09/2026
### 01/09/2026 - Fix Generals Base Game Compilation
- **Context**: Resolvido erro de compilação em `Generals` base game (`fatal error: 'GameNetwork/GeneralsOnline/NGMPGame.h' file not found`).
- **Causa Raiz**: O header `NGMPGame.h` e o ponteiro `TheNGMPGame` pertencem à camada de expansão (`GeneralsMD`), enquanto a integração NGMP está restrita ao Zero Hour por enquanto.
- **Correção**: Removido include e bloco de `TheNGMPGame` em `Generals/Code/GameEngine/Source/GameLogic/System/GameLogic.cpp`.
- **Validação**: Ambos os builds (`build-macos-generals.sh` e `build-macos-zh.sh`) compilaram com sucesso (Exit 0).

### 01/09/2026 - QuickMatch via NGMP Integration
- **Context**: Implementação do matchmaking ranqueado (QuickMatch) via NGMP em paridade com o repositório de referência.
- **Alterações Realizadas**:
  1. `OnlineServices_Manager.h/cpp`:
     - Adicionado método `widenMatchmakingAsync()` para o endpoint `POST /Matchmaking/Widen`.
     - Atualizado `startMatchmakingAsync()` com payload completo (`exe_crc`, `ini_crc`, `anticheat_id`) esperado pelo controller C#.
     - Adicionados eventos `EVENT_MATCHMAKING_MATCH_FOUND` e `EVENT_MATCHMAKING_MESSAGE`.
     - Implementados handlers WebSocket para `msg_id: 20` (`MATCHMAKING_ACTION_JOIN_PREARRANGED_LOBBY`), `msg_id: 21` (`MATCHMAKING_ACTION_START_GAME`) e `msg_id: 22` (`MATCHMAKING_MESSAGE`).
  2. `WOLQuickMatchMenu.cpp`:
     - Integrado carregamento assíncrono de playlists e mapas via `requestPlaylistsAsync()` e `getPlaylists()`.
     - Handlers de `buttonStartID`, `buttonStopID` e `buttonWidenID` conectados às APIs NGMP com validação de `MinSelectedMaps`.
     - Eventos WebSocket de Matchmaking despachados diretamente para a UI (mensagens de status no chat, áudio de match found e partida simultânea).
### 01/09/2026 - QuickMatch Lobby Join & Shutdown Fixes
- **Context**: Investigação e resolução de timeout ao encontrar partida no QuickMatch e crash ao fechar o jogo via Cmd+Q.
- **Causas Raizes e Soluções**:
  1. `Playlists 401 Unauthorized`: Adicionado cabeçalho `Authorization: Bearer` e parser robusto para suportar dicionário indexado de playlists.
  2. `Join Lobby HTTP 417 Expectation Failed`: O `joinLobbyAsync` enviava `"anticheat_id": -1`, divergindo do `AnticheatID = 0` (`NONE`) do lobby criado pelo matchmaking server. Corrigido para `0`.
  3. `Matchmaking Start HTTP 204`: Aceito status 204 (No Content) como resposta de sucesso em `startMatchmakingAsync`.
  4. `Segfault on Cmd+Q Shutdown`: Adicionadas verificações de nulidade para `TheGameSpyInfo`, `userData`, `TheShell` e `TheTransitionHandler` em `WOLQuickMatchMenuShutdown`, `WOLLobbyMenuShutdown` e `WOLGameSetupMenuShutdown`.
### 01/09/2026 - MapCache Slashes Normalization & Matchmaking Fixes
- **Context**: Investigação de falha na resolução do mapa sorteado no matchmaking (`Koujou Okawa v3\Koujou Okawa v3.map`).
- **Causa Raiz**: O servidor de matchmaking envia os caminhos de mapa com barras invertidas estilo Windows (`\`), enquanto o macOS e o filesystem indexam com barras normais (`/`). O `MapCache::findMap` não normalizava adequadamente os comparadores em `ends_with`, resultando em mapa não encontrado (`nullptr`).
- **Solução**:
  1. Refatorado `MapCache::findMap` em `MapUtil.cpp` para unificar normalização de barras (`\` para `/`), insensitive case e extração de folha base (`base leaf matching`).
  2. Mapas com caminhos Windows, caminhos absolutos Unix ou simples nomes de exibição agora são resolvidos com 100% de sucesso.
### 01/09/2026 - UserPreferences Backslash Path Fix & POSIX Normalization
- **Context**: Arquivos de preferências (`GeneralsOnline\*.ini`) e pastas (`GeneralsOnline\Ladders`) estavam sendo criados com contrabarras literais na raiz do diretório de dados em vez de dentro do subdiretório `GeneralsOnline/`.
- **Causa Raiz**: O `UserPreferences` utilizava chamada direta a `fopen` com caminhos no formato Windows `GeneralsOnline\\*.ini`, e `PeerDefs.cpp`/`LadderDefs.cpp` criavam diretórios com contrabarras literais que o kernel POSIX trata como caracteres válidos de nome de arquivo e não como separadores de pasta.
- **Solução**:
  1. `UserPreferences::load()` e `UserPreferences::write()` agora normalizam contrabarras (`\`) para barras (`/`) e asseguram a criação do diretório pai (`std::filesystem::create_directories`) antes de salvar qualquer `.ini`.
  2. Atualizados todos os caminhos hardcoded de `GeneralsOnline\\` para `GeneralsOnline/` em `UserPreferences.cpp`, `PeerDefs.cpp`, `LadderDefs.cpp`, `PersistentStorageThread.cpp`, `MainMenuUtils.cpp`, `PopupHostGame.cpp` e `PopupPlayerInfo.cpp` (com backport completo para o Generals base).
- **Validação**: Build Zero Hour concluído com Exit 0 e deploy efetuado em `~/GeneralsX/GeneralsZH`.
### 01/09/2026 - Multi-Directory User Maps Discovery & Disk Fallback
- **Context**: Investigação do erro de transferência/mapa não encontrado para mapas ranqueados sorteados pelo QuickMatch (ex: `[RANK] Vendetta ZH v1`).
- **Causa Raiz**: O `TheGlobalData->getPath_UserData()` no macOS aponta para `~/Library/Application Support/GeneralsX/GeneralsZH/`, e o `MapCache` apenas indexava a pasta `Maps` daquele diretório específico. Todos os 40 mapas do pacote NGMP estavam instalados no diretório padrão de documentos do usuário (`~/Documents/Command and Conquer Generals Zero Hour Data/Maps/`).
- **Solução**:
  1. `MapCache::updateCache()` agora descobre e indexa automaticamente mapas presentes em `~/Documents/Command and Conquer Generals Zero Hour Data/Maps/` e caminhos de sincronização em nuvem.
  2. Implementado fallback de busca direta em disco em `MapCache::findMap()`: se um mapa não estiver carregado na memória no momento da chamada, o motor busca o arquivo `.map` em disco, registra dinamicamente no cache e retorna seus metadados.
  3. Sincronizados todos os mapas customizados para `~/Library/Application Support/GeneralsX/GeneralsZH/Maps/`.
  4. Mantido o botão `Voltar` habilitado no estado de partida encontrada no menu de QuickMatch.
- **Validação**: Build Zero Hour compilado com Exit 0 e deploy executado em `~/GeneralsX/GeneralsZH`.
### 01/09/2026 - UserPreferences Filename Preservation & Country Selection Persistence
- **Context**: Popup de seleção de país/região (`GSOVERLAY_LOCALESELECT`) aparecia repetidamente a cada login no menu online, mesmo com `Locale = 27` salvo em `GSMiscPref*.ini`.
- **Causa Raiz**:
  1. No método `UserPreferences::load()`, a chamada a `AsciiString::getBufferForRead()` para normalização de barras em `normalizedFname` zerava o buffer da string para vazio `""`, fazendo o motor tentar abrir o diretório do usuário em vez do arquivo `.ini`.
  2. No `GlobalData.cpp` em ambientes POSIX, `BuildUserDataPathFromRegistry()` retornava o caminho sem a barra `/` no final, causando concatenações sem separador.
- **Solução**:
  1. `UserPreferences::load()` agora utiliza `std::string` para normalização de barras e validação de `TheGlobalData->getPath_UserData()`, preservando 100% dos nomes de arquivo (`GSMiscPref*.ini`, `Options.ini`, `CustomPref*.ini`, `QMPref*.ini`).
  2. `GlobalData::BuildUserDataPathFromRegistry()` (Zero Hour e Generals base) garante que `getPath_UserData()` sempre termine com `/` no macOS e Linux.
  3. Atualizada concatenação de caminhos de `map.ini` e `solo.ini` em `GameLogic.cpp` para formato portátil `/`.
- **Validação**: Verificação dos logs em execução confirmou carregamento bem-sucedido de `GSMiscPref1.ini`, leitura de `Locale = 27`, e supressão do diálogo repetido de seleção de país.


